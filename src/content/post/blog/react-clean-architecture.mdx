---
title: 리액트 클린 아키텍처 - 6개 레이어로 설계하는 유지보수성
date: 2025-11-09
updatedDate: 2025-11-09
tags:
  [
    리액트,
    React,
    클린 아키텍처,
    Clean Architecture,
    프론트엔드 아키텍처,
    Repository 패턴,
    Mapper,
    TanStack Query,
    Domain,
    Entity,
  ]
category: DEVELOPMENT
image: "/images/react-clean-architecture/thumbnail.webp"
---

우리 팀이 만드는 서비스는 외부 카드사와 협업하여 커스텀 디자인 카드를 제작하고, 이를 관리하는 `사내 SaaS 툴`이다. 문제는 카드사 API가 빈약했다는 점이다. 필요한 API는 제공되지 않고, 있는 API들은 우리 요구사항과 맞지 않았다. 결국 **여러 API를 조합**하거나, **불필요한 호출을 반복**하면서 어떻게든 기능을 만들어냈다.

더 큰 문제는 **기획이 자주 바뀌었다**는 점이다. 카드사의 API 개선 일정은 늘 불명확했고, 임시로 만든 코드가 쌓였다. 데이터 싱크를 맞추는 복잡한 로직이 곳곳에 흩어졌다. 팀원들은 잦은 변경에 지쳤고, 나중에 카드사가 제대로 된 API를 제공하면 빠르게 교체할 수 있도록 **외부 의존성을 격리**해야 했다.

서버나 앱 개발에서는 클린 아키텍처로 이런 문제를 해결한다. 하지만 리액트에서는 관련 자료가 많지 않았다. `Flutter`, `Kotlin`, `Swift` 같은 객체 지향 프론트엔드 생태계를 참고해 **우리 팀 상황에 맞는 클린 아키텍처**를 만들었다. 결과는 어땠을까? 외부 API가 바뀌어도 **`Data` 레이어만 수정**하면 되는 구조가 만들어졌다. 여러 API를 조합하던 복잡한 로직도 깔끔하게 정리됐다.

어떻게 이런 변화가 가능했을까? 먼저 기존 방식의 문제점부터 살펴보자.

---

## 기존 방식의 한계점

이런 사례로 한번 이해해보자. 결제 대행사 예치금으로 사용자에게 결제를 처리하는 컴포넌트가 있다.

```ts
// 클린 아키텍처를 적용하지 않은 코드
import { useQuery } from "@tanstack/react-query";
import axios from "axios";

interface Props {
  paymentGatewayId: number;
  userId: number;
  paymentAmount: number;
}

export function ProcessPaymentButton({
  paymentGatewayId,
  userId,
  paymentAmount,
}: Props) {
  // 🤔 한계 1: HTTP 클라이언트가 컴포넌트에 직접 의존
  const { data: paymentGateway } = useQuery({
    queryKey: ["paymentGateway", paymentGatewayId],
    queryFn: async () => {
      const response = await axios.get(
        `/api/payment-gateways/${paymentGatewayId}`
      );
      return response.data;
    },
  });

  const { mutate, isPending } = useMutation({
    mutationFn: async () => {
      // 🤔 한계 2: 검증 로직이 여러 곳에 중복
      if (!paymentGateway) {
        throw new Error("결제 대행사 정보를 불러올 수 없습니다");
      }

      // 🤔 한계 3: 서버 응답 형식에 직접 의존
      if (paymentGateway.wallet_balance <= 0) {
        throw new Error("예치금 잔액이 부족합니다");
      }

      if (paymentAmount < 1_000) {
        throw new Error("최소 결제 금액은 1,000원입니다");
      }

      // 🤔 한계 1: HTTP 클라이언트가 컴포넌트에 직접 의존
      const response = await axios.post("/api/payments/process", {
        payment_gateway_id: paymentGatewayId,
        user_id: userId,
        payment_amount: paymentAmount,
      });

      return response.data;
    },
    onSuccess: (data) => {
      // 🤔 한계 3: 서버 응답 형식에 직접 의존
      alert(`결제 처리 완료! 결제 ID: ${data.payment_id}`);
    },
    onError: (error) => {
      alert(error.message || "결제 처리 실패");
    },
  });

  return (
    <button onClick={() => mutate()} disabled={isPending}>
      {isPending ? "결제 중..." : "결제 처리"}
    </button>
  );
}
```

### 한계 1: HTTP 클라이언트 교체가 어렵다

```ts
// axios를 컴포넌트에서 직접 사용
const { data: paymentGateway } = useQuery({
  queryKey: ["paymentGateway", paymentGatewayId],
  queryFn: async () => {
    const response = await axios.get(
      `/api/payment-gateways/${paymentGatewayId}`
    );
    return response.data;
  },
});

const response = await axios.post("/api/payments/process", {
  payment_gateway_id: paymentGatewayId,
  user_id: userId,
  payment_amount: paymentAmount,
});
```

`axios`를 직접 사용하고 있다. 만약 `fetch`나 다른 HTTP 클라이언트로 바꾸고 싶다면, 해당 코드를 사용하는 모든 곳을 찾아 수정해야 한다. 테스트할 때도 `axios` 모킹, 서버 응답 형식, API 엔드포인트를 모두 알아야 해서 복잡하다.

### 한계 2: 비즈니스 규칙 변경 시 여러 파일 수정

최소 결제 금액 같은 비즈니스 규칙이 여러 컴포넌트에 흩어져 있다. 기획이 바뀌어서 "최소 금액을 1,000원에서 5,000원으로 변경해주세요"라는 요청이 오면, 해당 검증 로직을 사용하는 모든 곳을 찾아서 수정해야 한다.

### 한계 3: 서버 응답 형식 변경 시 영향 범위가 넓다

서버가 기획 변경이나 설계 변동으로 필드명을 바꾸면(예: `payment_gateway_id → gateway_id`), 해당 필드를 사용하는 모든 컴포넌트를 찾아서 수정해야 한다.

### 한계 4: 책임이 명확히 분리되지 않는다

"훅으로 분리하면 되지 않나?" 맞다. 훅으로 재사용은 해결할 수 있다. 하지만 **어디까지 분리**해야 할까?

```ts
// 훅으로만 분리하면 여전히 한계가 있다
function usePaymentProcess() {
  return useMutation({
    mutationFn: async (data) => {
      // 1. 검증 로직 (Domain)
      if (data.payment_amount < MINIMUM_AMOUNT) throw new Error();

      // 2. API 호출 (Infrastructure)
      const response = await axios.post("/api/payments", {
        payment_gateway_id: data.paymentGatewayId, // 3. 서버 형식 (Data)
        payment_amount: data.payment_amount,
      });

      return response.data;
    },
  });
}
```

이 훅은 **비즈니스 로직**(검증), **API 호출**(axios), **서버 형식**(`snake_case`)이 뒤섞여 있다.

- `axios` → `fetch` 변경 시: 훅 수정 필요
- 최소 금액 규칙 변경 시: 훅 수정 필요
- 서버 필드명 변경 시: 훅 수정 필요
- **단위 테스트**: API 모킹 없이 검증 로직만 테스트하기 어려움

훅은 **로직 재사용**을 해결한다. **책임 분리**도 할 수 있지만, 클린 아키텍처처럼 명확하게 분리하기는 쉽지 않다. 클린 아키텍처는 각 레이어의 **변경 이유를 명시적으로 분리**해서 이런 어려움을 극복한다.

---

리액트 프로젝트에서 클린 아키텍처를 어떻게 구현할까? 6개 레이어로 역할을 분리한 구조를 살펴보자:

![리액트 클린 아키텍처 - 6개 레이어로 역할을 분리해서 변경에 강한 구조 만들기](/images/react-clean-architecture/thumbnail.webp)

## 클린 아키텍처란?

클린 아키텍처는 이런 문제를 해결하기 위한 구조다. 핵심은 **역할을 분리**하는 것이다.

마치 온라인 쇼핑몰을 떠올려보자. 쇼핑몰에는 다음과 같은 역할이 있다:

- **고객**: 상품을 주문한다
- **재고 관리**: 재고를 확인하고 업데이트한다
- **결제 시스템**: 결제를 처리한다
- **배송 시스템**: 상품을 배달한다

각자의 역할이 명확하고, 서로 독립적이다. 결제 시스템이 바뀌어도 재고 관리는 영향을 받지 않는다. 배송 업체가 바뀌어도 고객 인터페이스는 그대로다.

클린 아키텍처도 마찬가지다. 코드를 **6개의 레이어**로 나누고, 각 레이어는 명확한 역할을 가진다.

### 6개 레이어 구조

```
┌─────────────────────────────────────────────────────────┐
│              Presentation Layer (React)                  │
│  Components, Pages, Hooks, State (Zustand + TanStack)   │
└────────────────────┬────────────────────────────────────┘
                     │ depends on
┌────────────────────▼────────────────────────────────────┐
│           Domain Layer (Business Logic)                  │
│     Use Cases, Entities, Repository Interfaces          │
└────────────────────▲────────────────────────────────────┘
                     │ implements
┌────────────────────┴────────────────────────────────────┐
│              Data Layer (API Access)                     │
│    Repository Impl, DTOs, Mappers, Data Sources         │
└────────────────────┬────────────────────────────────────┘
                     │ uses
┌────────────────────▼────────────────────────────────────┐
│         Infrastructure Layer (External Services)         │
│            HTTP Client (Axios), Storage                  │
└─────────────────────────────────────────────────────────┘

              Core Layer (Cross-cutting Concerns)
          Config, Constants, Utils, Errors, Validators
            ← 모든 레이어에서 참조 가능
```

- `DI`: 모든 레이어의 의존성을 조합하고 주입 (Dependency Injection Container)
- `Infrastructure`: 외부 세계와의 통신 (HTTP 클라이언트, 라이브러리)
- `Data`: 외부 데이터를 가져와서 내부 형식으로 변환
- `Domain`: 비즈니스 규칙과 로직 (UI, API 모름)
- `Presentation`: UI와 상호작용 (Hook, Component)
- `Core`: 모든 레이어에서 사용하는 공통 기능 (Constants, Utils, Errors, Validators)

핵심은 `Domain`이 **완전히 독립적**이라는 점이다. `Domain`은 UI가 어떻게 생겼는지, API가 어떤 형식인지 전혀 모른다. 오직 **비즈니스 규칙**만 담당한다.

### 의존성 방향

```
Presentation → Domain ← Data ← Infrastructure
       ↓          ↓        ↓          ↓
                  DI (모든 레이어를 조합)
       ↓          ↓        ↓          ↓
              Core (모든 레이어가 의존 가능)
```

의존성은 **항상 안쪽으로** 향한다. `Domain`은 `Core`만 의존하고, 나머지 레이어(`Data`, `Infrastructure`, `Presentation`)는 `Domain`을 의존한다.

`DI` 레이어는 가장 바깥쪽에 위치하여 모든 레이어의 구체적인 구현체를 조합하고 의존성을 주입한다. `Core` 레이어는 공통 유틸리티로, 모든 레이어에서 참조 가능하다.

**핵심 원칙**:

- `Domain`은 **순수하다**: `Core`를 제외한 어떤 레이어도 import하지 않는다
- **Repository Interface는 Domain에**: `Data` Layer는 Domain의 interface를 **구현**한다
- **의존성 역전**: 고수준(`Domain`)이 저수준(`Data`)에 의존하지 않는다

Uncle Bob이 제시한 클린 아키텍처의 공식 다이어그램을 살펴보자:

![Uncle Bob의 클린 아키텍처 다이어그램 - 동심원 구조로 레이어가 나뉘어져 있고, 의존성은 항상 안쪽으로 향한다](/images/react-clean-architecture/clean-architecture.webp)

위 다이어그램에서 핵심은 **의존성 방향**이다. 화살표가 모두 안쪽(중심의 Entities/Use Cases)을 가리킨다. 바깥쪽 레이어(UI, DB, 외부 서비스)는 안쪽을 의존하지만, 안쪽은 바깥을 전혀 모른다. 이것이 바로 `의존성 역전 원칙(Dependency Inversion Principle)`이다.

우리 프로젝트는 이 구조를 React 생태계에 맞게 6개 레이어로 재구성했다. 하지만 핵심 철학은 동일하다: `Domain`은 순수하고, 의존성은 항상 안쪽으로 흐른다.

> 💡 더 자세한 내용은 Uncle Bob의 [Clean Architecture 블로그](https://blog.cleancoder.com/uncle-bob/2012/08/13/the-clean-architecture.html)를 참고하자.

이게 왜 중요할까? `Domain`에 비즈니스 로직을 넣으면, UI가 바뀌든 API가 바뀌든 `Domain`은 영향을 받지 않는다. 마치 온라인 쇼핑몰에서 배송 업체를 변경해도 재고 관리나 결제 처리 로직은 그대로인 것처럼.

개념을 이해했으니, 이제 실제 프로젝트에서 어떻게 구현하는지 보자.

---

## 폴더 구조

실제 프로젝트에서 클린 아키텍처를 어떻게 폴더로 표현할까? 각 레이어의 역할이 명확히 드러나는 구조를 살펴보자.

### 소규모 프로젝트 구조 (권장)

프로젝트 초기 단계나 기능이 단순하다면 레이어별로 분리하는 것만으로 충분하다.

```
src/
├── core/                                 # 공통 기능 (모든 레이어에서 참조 가능)
│   ├── constants/
│   │   └── app-constants.ts              # 전역 상수 (통화, 날짜 형식 등)
│   ├── errors/
│   │   └── AppError.ts                   # 전역 에러 (모든 도메인 공통)
│   └── utils/
│       ├── date-utils.ts                 # 날짜 유틸
│       └── currency-utils.ts             # 통화 유틸
│
├── infrastructure/                       # 외부 서비스 (HTTP, Storage, Analytics)
│   ├── http/
│   │   ├── fetch-http-client.ts          # Fetch API 기반 HTTP 클라이언트
│   │   └── axios-http-client.ts          # Axios 기반 HTTP 클라이언트
│   ├── storage/
│   │   ├── local-storage.ts              # localStorage 추상화
│   │   └── session-storage.ts            # sessionStorage 추상화
│   └── configs/
│       └── tanstack-query.config.ts      # TanStack Query 설정
│
├── domain/                               # 비즈니스 로직 (외부 의존성 없음)
│   ├── config/
│   │   └── payment-constants.ts          # 결제 도메인 상수 (최소/최대 금액 등)
│   ├── types/
│   │   └── payment-types.ts              # 결제 관련 타입 정의
│   ├── entities/
│   │   ├── PaymentGateway.ts             # 결제 수단 Entity (boolean 메서드만)
│   │   └── User.ts                       # 사용자 Entity (boolean 메서드만)
│   ├── errors/
│   │   └── PaymentError.ts               # 결제 전용 에러
│   ├── repositories/
│   │   ├── PaymentGatewayRepository.ts   # Repository Interface (Data Layer가 구현)
│   │   └── UserRepository.ts             # Repository Interface
│   └── useCases/
│       ├── processPayment.ts             # 결제 처리 UseCase
│       └── processRefund.ts              # 환불 처리 UseCase
│
├── data/                                 # 데이터 접근 (Domain 인터페이스 구현)
│   ├── remote/
│   │   ├── api/
│   │   │   └── payment-api.ts            # API 엔드포인트 정의
│   │   └── dto/
│   │       ├── request/
│   │       │   └── payment-request.dto.ts     # 요청 DTO
│   │       └── response/
│   │           └── payment-response.dto.ts    # 응답 DTO (서버 형태 그대로)
│   ├── repository/
│   │   ├── datasource/
│   │   │   └── payment-datasource.ts     # API 호출 격리 (REST/GraphQL 전환 대비)
│   │   └── impl/
│   │       └── PaymentRepositoryImpl.ts  # Repository 구현체
│   └── mapper/
│       └── payment-mapper.ts             # DTO ↔ Entity 변환 (서버 변경 시 여기만 수정)
│
├── presentation/                         # UI 레이어 (React 컴포넌트, 훅)
│   ├── ui/
│   │   ├── components/                   # 재사용 가능한 UI 컴포넌트
│   │   │   └── PaymentButton.tsx
│   │   ├── sections/                     # 기능 단위 섹션 (Suspense 포함)
│   │   │   └── PaymentSection.tsx
│   │   └── views/                        # 페이지 레이아웃
│   │       └── PaymentView.tsx
│   ├── hooks/
│   │   └── queries/
│   │       └── usePayments.ts            # TanStack Query 훅 (UseCase 호출)
│   ├── adapters/
│   │   └── payment-adapter.ts            # Entity → UI 변환 (한글 텍스트, 포맷팅)
│   ├── types/
│   │   └── payment-ui.types.ts           # UI 전용 타입
│   ├── constants/
│   │   └── query-keys.ts                 # TanStack Query Key 상수
│   ├── schemas/
│   │   └── payment-form.schema.ts        # Zod 폼 검증 스키마
│   └── utils/
│       └── format-currency.ts            # UI 포맷팅 유틸
│
└── di/                                   # 의존성 주입
    └── DIContainer.ts                    # 모든 의존성을 관리하는 컨테이너
```

### 중대규모 프로젝트 구조 (도메인 분리)

기능이 복잡하고 다양해지면 도메인별로 폴더를 분리할 수 있다.

```
src/
├── domains/
│   ├── payment/                          # 결제 도메인
│   │   ├── di/                           # 의존성 주입 컨테이너
│   │   │   └── payment-container.ts
│   │   ├── domain/                       # 비즈니스 로직
│   │   │   ├── config/                   # 도메인 상수
│   │   │   │   └── payment-constants.ts
│   │   │   ├── types/                    # 도메인 타입
│   │   │   ├── entities/                 # 도메인 엔티티
│   │   │   ├── repositories/             # Repository 인터페이스
│   │   │   └── useCases/                 # 비즈니스 유스케이스
│   │   ├── data/                         # 데이터 접근
│   │   │   ├── remote/
│   │   │   │   ├── api/                  # API 호출
│   │   │   │   └── dto/                  # 데이터 전송 객체
│   │   │   │       ├── request/
│   │   │   │       └── response/
│   │   │   ├── repository/
│   │   │   │   ├── datasource/           # 데이터 소스
│   │   │   │   └── impl/                 # Repository 구현체
│   │   │   └── mapper/                   # DTO → Entity 변환
│   │   └── presentation/                 # UI 레이어
│   │       ├── ui/
│   │       │   ├── views/                # 페이지 레벨 레이아웃
│   │       │   ├── sections/             # 기능 섹션
│   │       │   └── components/           # 재사용 컴포넌트
│   │       ├── adapters/                 # Entity → UI 타입 변환 (7개+ 메서드)
│   │       ├── hooks/
│   │       │   └── queries/              # TanStack Query 훅
│   │       ├── types/                    # UI 전용 타입
│   │       ├── constants/                # 쿼리 키, 상수
│   │       ├── schemas/                  # 검증 스키마 (Zod 등)
│   │       └── utils/                    # Presentation 유틸리티
│   │
│   ├── user/                             # 사용자 도메인
│   │   ├── di/
│   │   ├── domain/
│   │   ├── data/
│   │   └── presentation/
│   │
│   └── cafeteria/                        # 식당 도메인
│       ├── di/
│       ├── domain/
│       ├── data/
│       └── presentation/
│
├── core/                                 # 전역 공통 기능
│   ├── constants/                        # 전역 상수 (모든 도메인이 사용)
│   ├── errors/                           # 전역 에러
│   └── utils/                            # 전역 유틸리티
│
└── infrastructure/                       # 외부 서비스
    ├── http/                             # HTTP 클라이언트
    ├── storage/                          # 로컬 스토리지
    └── configs/                          # 환경 설정
```

### 어떤 구조를 선택해야 할까?

**소규모 구조로 시작하는 것을 권장한다.** 다음 기준을 참고하자:

| 프로젝트 상황 | 권장 구조 | 이유 |
|------------|---------|------|
| **초기 단계, 단순한 CRUD** | 소규모 (레이어 분리) | 빠른 개발, 팀원 누구나 쉽게 이해 |
| **기능이 복잡하고 다양해지면** | 도메인 분리 검토 | 파일이 많아지고 팀 분업이 필요할 때 |
| **명확한 도메인 경계 존재** | 도메인 분리 | 결제/사용자/식당처럼 독립적 개발이 필요할 때 |

**도메인 분리의 장점**:
- 각 도메인이 독립적으로 개발 가능
- 팀원별로 도메인 담당 분업이 쉬움
- 마이크로 프론트엔드 전환 시 유리

**도메인 분리의 단점**:
- 폴더 구조가 복잡해짐
- 도메인 경계 설정이 어려울 수 있음 (결제와 사용자를 분리해야 할까?)

대부분의 프로젝트는 **소규모 구조로 시작**하고, 기능이 늘어나면 **도메인별로 점진적으로 분리**하는 것을 권장한다.

---

이제 각 레이어의 역할과 실제 코드를 살펴보자. 가장 바깥쪽 레이어부터 차례대로 깊이 들어가보자.

---

## Core Layer: 공통 기능의 중심

`Core` 레이어는 조금 특별하다. **모든 레이어에서 참조할 수 있는 공통 기능**을 담고 있다. 일반적으로 클린 아키텍처에서는 의존성이 안쪽으로 향하지만, `Core`는 예외다.

왜 필요할까? 상수, 에러, 유틸리티 함수처럼 모든 레이어에서 사용하는 코드를 매번 각 레이어에 중복해서 만들 필요가 있을까? 그렇지 않다. `Core`에 한 번만 정의하고 모든 레이어에서 가져다 쓰면 된다.

### Constants: 전역 상수

`Core`에는 **모든 도메인이 사용하는 전역 상수**만 담는다. 특정 도메인만 사용하는 상수는 `domain/config/`에 넣어야 한다.

```ts
// core/constants/app-constants.ts
export const CURRENCY_UNIT = "원"; // 통화 단위 (모든 도메인에서 사용)
export const DATE_FORMAT = "YYYY-MM-DD"; // 날짜 형식
export const DEFAULT_PAGE_SIZE = 20; // 기본 페이지 크기
```

결제 관련 비즈니스 규칙은 `Domain` 레이어에 위치한다:

```ts
// domain/config/payment-constants.ts
export const MIN_PAYMENT_AMOUNT = 1_000; // 최소 결제 금액
export const MAX_PAYMENT_AMOUNT = 1_000_000; // 최대 결제 금액
```

### Errors: 커스텀 에러

```ts
// core/errors/AppError.ts
export class AppError extends Error {
  constructor(public code: string, message: string) {
    super(message);
    this.name = "AppError";
  }
}

export class InsufficientBalanceError extends AppError {
  constructor() {
    super("INSUFFICIENT_BALANCE", "잔액이 부족하다");
  }
}
```

### 에러는 어느 레이어에 넣어야 할까?

**Core 에러**: 모든 도메인이 사용하는 전역 에러

```ts
// core/errors/AppError.ts
export class AppError extends Error { ... }
export class NetworkError extends AppError { ... }
export class UnauthorizedError extends AppError { ... }
```

**Domain 에러**: 특정 도메인에만 관련된 에러

```ts
// domain/errors/PaymentError.ts
export class InsufficientBalanceError extends AppError {
  constructor(required: number, current: number) {
    super(`잔액 부족: 필요 ${required}원, 현재 ${current}원`);
  }
}

export class InvalidPaymentAmountError extends AppError { ... }
```

**원칙**: 모든 곳에서 쓰면 Core, 특정 도메인만 쓰면 Domain

---

### Validators: 검증 로직

검증 로직도 마찬가지다. **전역 검증**은 `Core`에, **도메인 검증**은 `Domain`에 위치한다.

```ts
// domain/validators/payment-validator.ts
import { MIN_PAYMENT_AMOUNT, MAX_PAYMENT_AMOUNT } from "../config/payment-constants";

export const validatePaymentAmount = (amount: number): void => {
  if (amount < MIN_PAYMENT_AMOUNT) {
    throw new Error(`최소 결제 금액은 ${MIN_PAYMENT_AMOUNT}원입니다`);
  }
  if (amount > MAX_PAYMENT_AMOUNT) {
    throw new Error(`최대 결제 금액은 ${MAX_PAYMENT_AMOUNT}원입니다`);
  }
};
```

### Utils: 공통 유틸리티

```ts
// core/utils/formatters.ts
export const formatCurrency = (amount: number): string => {
  return amount.toLocaleString("ko-KR");
};

export const formatPayment = (payment: number): string => {
  return `${formatCurrency(payment)}원`;
};
```

`Core` 레이어는 **순수 함수와 상수**만 담는다. 비즈니스 로직이나 외부 의존성을 가지면 안 된다. 그래야 모든 레이어에서 안전하게 사용할 수 있다.

---

## Infrastructure Layer: 외부 세계와의 격리

`Infrastructure` 레이어는 외부 라이브러리나 HTTP 클라이언트를 추상화한다. 왜 필요할까?

만약 `axios`를 직접 쓰다가 `fetch`로 바꾸고 싶다면? 모든 코드를 수정해야 한다. 하지만 추상화해두면 구현체만 바꾸면 된다.

```ts
// infrastructure/http/httpClient.ts
export interface HttpClient {
  get<T>(url: string): Promise<T>;
  post<T>(url: string, data: unknown): Promise<T>;
  put<T>(url: string, data: unknown): Promise<T>;
  delete<T>(url: string): Promise<T>;
}
```

이건 `인터페이스`다. 구현체는 따로 만든다.

```ts
// infrastructure/http/axiosHttpClient.ts
import axios from "axios";
import type { HttpClient } from "./httpClient";

export class AxiosHttpClient implements HttpClient {
  async get<T>(url: string): Promise<T> {
    const response = await axios.get(url);
    return response.data;
  }

  async post<T>(url: string, data: unknown): Promise<T> {
    const response = await axios.post(url, data);
    return response.data;
  }

  // put, delete도 유사하게 구현
}
```

이제 `axios`에서 `fetch`로 바꾸고 싶으면? 새로운 구현체만 만들면 된다.

```ts
// infrastructure/http/fetchHttpClient.ts
import type { HttpClient } from "./httpClient";

export class FetchHttpClient implements HttpClient {
  async get<T>(url: string): Promise<T> {
    const response = await fetch(url);
    return response.json();
  }

  async post<T>(url: string, data: unknown): Promise<T> {
    const response = await fetch(url, {
      method: "POST",
      body: JSON.stringify(data),
      headers: { "Content-Type": "application/json" },
    });
    return response.json();
  }

  // put, delete도 유사하게 구현
}
```

나머지 코드는 `HttpClient` 인터페이스만 의존하므로 **아무 영향을 받지 않는다**.

---

## Data Layer: 외부 데이터를 내부 형식으로 변환

`Data` 레이어는 **Domain의 Repository Interface를 구현**하고, **DTO를 Entity로 변환**한다.

**중요한 원칙**:

- **Repository Implementation은 Data에**: Domain의 interface를 `implements`한다
- **DTO → Entity 변환은 Data의 책임**: Mapper를 통해 변환한다
- **Domain과 Infrastructure를 연결**: Domain은 "무엇을", Data는 "어떻게"를 담당한다

`Data` 레이어는 4가지 역할을 한다:

1. `Repository Impl`: Domain의 Repository Interface를 구현한다
2. `DataSource`: API를 호출하고 DTO를 반환한다
3. `DTO(Data Transfer Object)`: 서버 응답 타입을 정의한다
4. `Mapper`: DTO를 `Domain`의 `Entity`로 변환한다

### Repository: API 호출 로직

```ts
// data/repositories/PaymentGatewayRepositoryImpl.ts
import type { PaymentGatewayRepository } from "@/domain/repositories/PaymentGatewayRepository";
import type { PaymentGatewayDataSource } from "@/data/datasources/paymentGatewayDataSource";
import type { PaymentGateway } from "@/domain/entities/PaymentGateway";
import type { User } from "@/domain/entities/User";
import { mapPaymentGatewayDtoToDomain } from "@/data/mappers/paymentGatewayMapper";
import { mapUserDtoToDomain } from "@/data/mappers/userMapper";

export class PaymentGatewayRepositoryImpl implements PaymentGatewayRepository {
  constructor(private paymentGatewayDataSource: PaymentGatewayDataSource) {}

  async getPaymentGateway(paymentGatewayId: number): Promise<PaymentGateway> {
    const paymentGatewayDto =
      await this.paymentGatewayDataSource.getPaymentGateway(paymentGatewayId);
    return mapPaymentGatewayDtoToDomain(paymentGatewayDto);
  }

  /**
   * 결제를 처리하고 업데이트된 사용자 정보를 반환한다.
   * 서버는 결제 처리 후 변경된 User 정보를 응답으로 보낸다.
   */
  async processPayment(
    paymentGatewayId: number,
    userId: number,
    paymentAmount: number
  ): Promise<User> {
    const userDto = await this.paymentGatewayDataSource.processPayment(
      paymentGatewayId,
      userId,
      paymentAmount
    );
    return mapUserDtoToDomain(userDto);
  }
}
```

### Data가 Domain에 의존하는 것은 DI 위반이 아니다

코드를 보면 `PaymentGatewayRepositoryImpl`이 Domain의 인터페이스를 import한다:

```ts
import type { PaymentGatewayRepository } from "@/domain/repositories/PaymentGatewayRepository";
```

이게 DI 위반 아닌가? **아니다!**

- ❌ DI 위반: `구체 클래스`에 의존
- ✅ DI 준수: `인터페이스`에 의존

Data Layer는 Domain의 `인터페이스`를 구현하는 것이므로 정상이다.

**핵심**: Domain은 Data를 전혀 모른다. Domain은 "PaymentGatewayRepositoryImpl"의 존재조차 모른다. Domain은 "PaymentGatewayRepository 인터페이스"만 안다.

이것이 `의존성 역전 원칙(Dependency Inversion Principle)`이다.

---

```ts
// data/repositories/UserRepositoryImpl.ts
import type { UserRepository } from "@/domain/repositories/UserRepository";
import type { UserDataSource } from "@/data/datasources/userDataSource";
import type { User } from "@/domain/entities/User";
import { mapUserDtoToDomain } from "@/data/mappers/userMapper";

export class UserRepositoryImpl implements UserRepository {
  constructor(private userDataSource: UserDataSource) {}

  async getUser(userId: number): Promise<User> {
    const userDto = await this.userDataSource.getUser(userId);
    return mapUserDtoToDomain(userDto);
  }

  async processPayment(userId: number, paymentAmount: number): Promise<User> {
    const userDto = await this.userDataSource.processPayment(
      userId,
      paymentAmount
    );
    return mapUserDtoToDomain(userDto);
  }

  async convertPoints(userId: number, paymentAmount: number): Promise<User> {
    const userDto = await this.userDataSource.convertPoints(
      userId,
      paymentAmount
    );
    return mapUserDtoToDomain(userDto);
  }
}
```

**중요한 원칙**:

- **Repository interface는 `Domain`에, implementation은 `Data`에**: `Domain`은 "어떻게 데이터를 가져올지"는 몰라도 된다
- **DTO → Entity 변환은 `Repository`의 책임**: `UseCase`는 `Entity`만 받는다
- **`DataSource`를 통해 데이터를 가져온다**: API 호출은 `DataSource`가 담당한다

그렇다면 `DataSource`는 무엇일까?

### DataSource: 네트워크 통신 담당

`DataSource`는 **API 엔드포인트를 호출하고 DTO를 반환하는 역할**을 한다.

마치 물류센터 직원이 선반에서 상품을 집어 박스에 담기만 하는 것처럼, `DataSource`는 API를 호출해서 DTO를 그대로 반환하기만 한다.

### DataSource를 분리하는 이유

`DataSource`는 "데이터를 어디서 가져올지"를 격리한다. 지금은 REST API지만 나중에 GraphQL, Firebase, Supabase로 바뀔 수 있다.

```ts
// 시나리오 1: REST → GraphQL 전환
class GraphQLPaymentDataSource {
  async getPaymentGateway(id: number) {
    const query = `{ paymentGateway(id: ${id}) { id balance currency } }`;
    return this.graphqlClient.query(query);
  }
}

// 시나리오 2: REST → Firebase 전환
class FirebasePaymentDataSource {
  async getPaymentGateway(id: number) {
    const doc = await firebase.firestore()
      .collection('payment_gateways')
      .doc(id.toString())
      .get();
    return doc.data();
  }
}

// 시나리오 3: REST → Supabase 전환
class SupabasePaymentDataSource {
  async getPaymentGateway(id: number) {
    const { data } = await supabase
      .from('payment_gateways')
      .select('*')
      .eq('id', id)
      .single();
    return data;
  }
}
```

이런 변경이 있어도 `Repository`, `Mapper`, `UseCase`는 전혀 영향받지 않는다. **`DataSource`만 교체하면 된다.**

### DataSource를 생략할 수도 있다

프로젝트가 작고, API가 단순하며, 데이터 소스 전환 가능성이 전혀 없다면 `Repository`에서 직접 호출해도 된다:

```ts
// DataSource 생략 버전
export class PaymentRepositoryImpl implements PaymentRepository {
  constructor(private httpClient: HttpClient) {}

  async getPaymentGateway(id: number): Promise<PaymentGateway> {
    const dto = await this.httpClient.get(`/api/payment-gateways/${id}`);
    return mapDtoToDomain(dto); // 바로 변환
  }
}
```

**우리 프로젝트**: 중간 규모이고 확장 가능성을 고려해서 `DataSource` 레이어 유지 ✅

```ts
// data/datasources/paymentGatewayDataSource.ts
import type { HttpClient } from "@/infrastructure/http/HttpClient";
import type { PaymentGatewayDto } from "@/data/dto/paymentGatewayDto";

export class PaymentGatewayDataSource {
  constructor(private httpClient: HttpClient) {}

  async getPaymentGateway(
    paymentGatewayId: number
  ): Promise<PaymentGatewayDto> {
    return this.httpClient.get<PaymentGatewayDto>(
      `/api/payment-gateways/${paymentGatewayId}`
    );
  }

  async processPayment(
    paymentGatewayId: number,
    userId: number,
    paymentAmount: number
  ): Promise<PaymentGatewayDto> {
    return this.httpClient.post<PaymentGatewayDto>("/api/payments/process", {
      payment_gateway_id: paymentGatewayId,
      user_id: userId,
      payment_amount: paymentAmount,
    });
  }
}
```

```ts
// data/datasources/userDataSource.ts
import type { HttpClient } from "@/infrastructure/http/HttpClient";
import type { UserDto } from "@/data/dto/userDto";

export class UserDataSource {
  constructor(private httpClient: HttpClient) {}

  async getUser(userId: number): Promise<UserDto> {
    return this.httpClient.get<UserDto>(`/api/users/${userId}`);
  }

  async convertPoints(userId: number, paymentAmount: number): Promise<UserDto> {
    return this.httpClient.post<UserDto>("/api/users/convert-points", {
      user_id: userId,
      payment_amount: paymentAmount,
    });
  }
}
```

### DataSource는 인터페이스가 필요할까?

**대부분은 `구체 클래스`로 충분**하다. 하지만 데이터 소스를 전환할 가능성이 있다면 인터페이스를 만든다.

```ts
// ✅ 인터페이스가 필요한 경우: 여러 구현체 존재
interface PaymentDataSource {
  getPaymentGateway(id: number): Promise<PaymentGatewayDto>;
}

class RestPaymentDataSource implements PaymentDataSource { ... }
class GraphQLPaymentDataSource implements PaymentDataSource { ... }
class FirebasePaymentDataSource implements PaymentDataSource { ... }
```

```ts
// ✅ 구체 클래스로 충분한 경우: 구현체 1개만 존재
export class PaymentGatewayDataSource {
  constructor(private httpClient: HttpClient) {}
  async getPaymentGateway(id: number) { ... }
}
```

**우리 프로젝트**: REST API만 사용하므로 `구체 클래스` ✅

---

`DataSource`는 3가지 원칙을 따른다:

1. **DTO만 반환한다**: `Entity`가 아닌 `DTO`를 반환한다. 변환은 `Mapper`의 몫이다.
2. **`HttpClient` 인터페이스에 의존한다**: `axios`나 `fetch`를 직접 사용하지 않는다. `HttpClient` 인터페이스를 통해 HTTP 클라이언트를 추상화한다.
3. **비즈니스 로직을 포함하지 않는다**: API 호출만 담당한다.

`DataSource`와 `HttpClient`의 관계를 보면, `axios`인지 `fetch`인지는 `DataSource`도 모른다. 이것이 바로 의존성 역전 원칙이다.

백엔드가 바뀌면 어떻게 될까? 처음엔 Spring Boot를 쓰다가 Firebase로 바꾸거나, Nest.js로 전환하거나, Supabase로 마이그레이션한다면? **`DataSource` 덕분에 걱정할 필요가 없다.** `Repository`, `Mapper`, `Entity`는 건드리지 않고 `DataSource`만 교체하면 된다. 이것이 바로 클린 아키텍처의 핵심 장점이다.

### DTO: 서버 응답 타입

서버가 보내는 데이터 형식을 정의한다.

```ts
// data/dto/paymentGatewayDto.ts
export interface PaymentGatewayDto {
  payment_gateway_id: number; // PG사 고유 ID
  payment_gateway_name: string; // PG사 이름
  wallet_balance: number; // 예치금 잔액
  created_at: string; // 생성 시각
  updated_at: string; // 수정 시각
}

// data/dto/userDto.ts
export interface UserDto {
  user_id: number; // 사용자 고유 ID
  user_name: string; // 사용자 이름
  payment_gateway_id: number; // 사용 중인 PG사 ID
  payment_balance: number; // 결제 잔액
  wallet_balance: number; // 지갑 잔액
  created_at: string; // 생성 시각
  updated_at: string; // 수정 시각
}

// data/dto/paymentTransactionDto.ts
export interface PaymentTransactionDto {
  transaction_id: string; // 거래 고유 ID
  user_id: number; // 사용자 ID
  payment_gateway_id: number; // PG사 ID
  transaction_type: "process" | "convert"; // 거래 유형
  amount: number; // 거래 금액
  balance_after: number; // 거래 후 잔액
  created_at: string; // 거래 시각
}
```

> **참고**: 모노레포 환경이라면 `packages/types`처럼 별도 패키지로 분리할 수 있다. 서버가 Swagger 문서를 완전하게 작성했다면 [`openapi-typescript`](https://openapi-ts.dev/)나 [`orval`](https://orval.dev/) 같은 도구로 타입을 자동 생성하는 것을 추천한다. 직접 선언하면 휴먼 에러가 발생할 수 있기 때문이다.

그런데 서버 팀에서 갑자기 `"기획이 바뀌어서 payment_gateway_id 필드를 gateway_id로 변경했습니다"`라고 통보하면 어떻게 될까? 기존 방식이라면 해당 필드를 사용하는 모든 컴포넌트를 찾아서 수정해야 한다. 하지만 클린 아키텍처에서는 단 **한 곳**만 고치면 된다. 바로 `Mapper`다.

---

### Mapper: 서버 변경의 충격을 흡수하는 완충재

`Mapper`는 서버와 우리 코드 사이의 **번역기**다. 서버가 `payment_gateway_id`를 보내든 `gateway_id`를 보내든, 우리 코드는 전혀 신경 쓰지 않는다. `Mapper`가 모든 변경을 **한 곳에서 처리**하기 때문이다.

마치 해외에서 산 전자제품을 사용할 때 **멀티 어댑터**를 쓰는 것과 비슷하다. 해외 전자제품(서버)의 플러그 모양이 바뀌어도, 멀티 어댑터(Mapper)만 있으면 우리 콘센트(Domain, Presentation)는 그대로 쓸 수 있다.

Mapper는 class가 아닌 `순수 함수`로 구현한다. 상태를 가질 필요가 없고, 단순히 데이터를 변환하는 역할만 하기 때문이다.

```ts
// data/mappers/paymentGatewayMapper.ts
import { z } from "zod";
import { PaymentGateway } from "@/domain/entities/PaymentGateway";
import type { PaymentGatewayDto } from "@/data/dto/paymentGatewayDto";

// 1. DTO 검증 스키마
const paymentGatewayDtoSchema = z.object({
  payment_gateway_id: z.number(),
  payment_gateway_name: z.string(),
  wallet_balance: z.number(),
  created_at: z.string(),
  updated_at: z.string(),
});

// DTO → Entity 변환 함수
export function mapPaymentGatewayDtoToDomain(
  dto: PaymentGatewayDto
): PaymentGateway {
  // 1. DTO 검증
  const validated = paymentGatewayDtoSchema.parse(dto);

  // 2. Entity 생성 (snake_case → camelCase)
  return new PaymentGateway({
    id: validated.payment_gateway_id,
    name: validated.payment_gateway_name,
    walletBalance: validated.wallet_balance,
    createdAt: new Date(validated.created_at),
    updatedAt: new Date(validated.updated_at),
  });
}

// Entity → DTO 변환 함수 (API 요청 시)
export function mapPaymentGatewayDomainToDto(
  paymentGateway: PaymentGateway
): PaymentGatewayDto {
  return {
    payment_gateway_id: paymentGateway.id,
    payment_gateway_name: paymentGateway.name,
    wallet_balance: paymentGateway.walletBalance,
    created_at: paymentGateway.createdAt.toISOString(),
    updated_at: paymentGateway.updatedAt.toISOString(),
  };
}
```

```ts
// data/mappers/userMapper.ts
import { z } from "zod";
import { User } from "@/domain/entities/User";
import type { UserDto } from "@/data/dto/userDto";

const userDtoSchema = z.object({
  user_id: z.number(),
  user_name: z.string(),
  payment_gateway_id: z.number(),
  payment_balance: z.number(),
  wallet_balance: z.number(),
  created_at: z.string(),
  updated_at: z.string(),
});

export function mapUserDtoToDomain(dto: UserDto): User {
  const validated = userDtoSchema.parse(dto);

  return new User({
    id: validated.user_id,
    name: validated.user_name,
    gatewayId: validated.payment_gateway_id,
    paymentBalance: validated.payment_balance,
    walletBalance: validated.wallet_balance,
    createdAt: new Date(validated.created_at),
    updatedAt: new Date(validated.updated_at),
  });
}
```

```ts
// data/mappers/paymentTransactionMapper.ts
import { z } from "zod";
import { PaymentTransaction } from "@/domain/entities/PaymentTransaction";
import type { PaymentTransactionDto } from "@/data/dto/paymentTransactionDto";

const paymentTransactionDtoSchema = z.object({
  transaction_id: z.string(),
  user_id: z.number(),
  payment_gateway_id: z.number(),
  transaction_type: z.enum(["process", "convert"]),
  amount: z.number(),
  balance_after: z.number(),
  created_at: z.string(),
});

export function mapPaymentTransactionDtoToDomain(
  dto: PaymentTransactionDto
): PaymentTransaction {
  const validated = paymentTransactionDtoSchema.parse(dto);

  return new PaymentTransaction({
    id: validated.transaction_id,
    userId: validated.user_id,
    paymentGatewayId: validated.payment_gateway_id,
    type: validated.transaction_type === "process" ? "payment" : "refund",
    amount: validated.amount,
    balanceAfter: validated.balance_after,
    createdAt: new Date(validated.created_at),
  });
}
```

`Mapper` 함수는 다음을 수행한다:

1. **검증**: `Zod`로 DTO의 형식을 검증한다
2. **필드명 매핑**: 서버 필드(`payment_gateway_id`) → 도메인 필드(`id`)로 변환한다
3. **네이밍 변환**: `snake_case` → `camelCase`로 변환한다
4. **타입 변환**: `string` → `Date`처럼 타입을 변환한다
5. **Entity 생성**: `Domain`의 `PaymentGateway`, `User`, `PaymentTransaction` Entity를 생성한다

**핵심은 이것이다**: 서버가 `payment_gateway_id → gateway_id`로 필드명을 바꾸면?

```ts
// ✅ Mapper 한 곳만 수정
export function mapPaymentGatewayDtoToDomain(dto: PaymentGatewayDto): PaymentGateway {
  return new PaymentGateway({
-   id: validated.payment_gateway_id,  // 기존
+   id: validated.gateway_id,           // 변경
    name: validated.payment_gateway_name,
    walletBalance: validated.wallet_balance,
    // ...
  });
}
```

`Mapper` 함수 **딱 1줄**만 고치면 된다. `Presentation`, `Domain`, `UseCase` 레이어는 전혀 수정하지 않아도 된다. 도입부에서 언급한 "10개 파일 수정 → 1개 파일 수정"이 바로 이것이다.

---

## Domain Layer: 비즈니스 규칙의 중심

`Domain` 레이어는 클린 아키텍처의 **핵심**이다. UI도 모르고, API도 모르고, 오직 **비즈니스 규칙**만 담당한다.

**중요한 원칙**:

- **Domain은 Core만 import한다**: `Data`, `Infrastructure`, `Presentation` 레이어는 절대 import하지 않는다
- **Repository Interface는 Domain에 정의한다**: 구현체(`Impl`)는 `Data` Layer에 위치한다

`Domain`은 3가지로 구성된다:

1. `Entity`: 비즈니스 규칙을 담은 객체
2. `Repository Interface`: 데이터 접근 추상화
3. `UseCase`: 비즈니스 로직의 흐름

### Entity: 비즈니스 규칙

Entity는 비즈니스와 관련된 **모든 규칙**을 담는다. 결제 대행사 시스템에는 `PaymentGateway`와 `User` Entity가 있다.

#### PaymentGateway Entity

`PaymentGateway` Entity는 결제 대행사의 예치금과 결제 처리 규칙을 담당한다.

```ts
// domain/entities/PaymentGateway.ts
interface PaymentGatewayProps {
  id: number;
  name: string;
  walletBalance: number;
  createdAt: Date;
  updatedAt: Date;
}

export class PaymentGateway {
  private readonly _id: number;
  private readonly _name: string;
  private readonly _walletBalance: number;
  private readonly _createdAt: Date;
  private readonly _updatedAt: Date;

  static readonly MIN_PAYMENT_AMOUNT = 1_000;
  static readonly MAX_PAYMENT_AMOUNT = 1_000_000;

  constructor(props: PaymentGatewayProps) {
    this._id = props.id;
    this._name = props.name;
    this._walletBalance = props.walletBalance;
    this._createdAt = props.createdAt;
    this._updatedAt = props.updatedAt;

    // 생성 시점에 필수 검증 수행
    this.validate();
  }

  private validate(): void {
    if (this._id <= 0) throw new Error("결제 대행사 ID는 양수여야 합니다");
    if (!this._name?.trim()) throw new Error("결제 대행사 이름은 필수입니다");
    if (this._walletBalance < 0)
      throw new Error("예치금 잔액은 음수일 수 없습니다");
  }

  get id() {
    return this._id;
  }
  get name() {
    return this._name;
  }
  get walletBalance() {
    return this._walletBalance;
  }

  // 비즈니스 규칙: 예치금 잔액 확인
  canProcessPayment(amount: number): boolean {
    return this._walletBalance >= amount;
  }

  // 사용자 입력 검증 (UseCase에서 호출)
  static validatePaymentAmount(amount: number): void {
    if (amount < this.MIN_PAYMENT_AMOUNT) {
      throw new Error(`최소 결제 금액은 ${this.MIN_PAYMENT_AMOUNT}원입니다`);
    }
    if (amount > this.MAX_PAYMENT_AMOUNT) {
      throw new Error(`최대 결제 금액은 ${this.MAX_PAYMENT_AMOUNT}원입니다`);
    }
  }

  static validateDepositAmount(amount: number): void {
    if (amount <= 0) {
      throw new Error("충전 금액은 0원보다 커야 합니다");
    }
  }
}
```

**Validation 전략**

| 패턴                   | 목적                         | 호출 시점               |
| ---------------------- | ---------------------------- | ----------------------- |
| `private validate()`   | Entity의 필수 불변 규칙 검증 | Entity 생성 시 자동     |
| `static validateXXX()` | 사용자 입력 사전 검증        | UseCase에서 명시적 호출 |

**핵심**: Constructor validation은 잘못된 데이터로 Entity가 생성되는 것을 원천 차단한다. Entity를 받는 모든 코드가 "이 객체는 유효하다"고 신뢰할 수 있다

### Entity 메서드는 boolean을 반환한다

Entity 메서드는 `판단 결과(boolean)`를 반환한다. 텍스트나 포맷팅은 Presentation의 Adapter 역할이다.

```ts
// ✅ 좋은 예: boolean 반환
class PaymentGateway {
  canProcessPayment(amount: number): boolean {
    return this.balance >= amount;
  }
}

// ❌ 나쁜 예: 텍스트 반환 (Adapter 역할 침범)
class PaymentGateway {
  getPaymentStatusMessage(amount: number): string {
    return this.balance >= amount
      ? "결제 가능합니다"
      : "잔액이 부족합니다";
  }
}
```

**원칙**:
- Entity → boolean 판단
- Adapter → 텍스트 변환

---

#### User Entity

`User` Entity는 사용자의 결제 내역과 지갑 잔액 관리 규칙을 담당한다.

```ts
// domain/entities/User.ts
interface UserProps {
  id: number;
  name: string;
  gatewayId: number;
  paymentBalance: number; // 결제 잔액
  walletBalance: number; // 지갑 잔액
  createdAt: Date;
  updatedAt: Date;
}

export class User {
  private readonly _id: number; // 사용자 고유 ID
  private readonly _name: string; // 사용자 이름
  private readonly _gatewayId: number; // PG사 ID
  private _paymentBalance: number; // 결제 잔액
  private _walletBalance: number; // 지갑 잔액
  private readonly _createdAt: Date; // 등록 시각
  private _updatedAt: Date; // 업데이트 시각

  constructor(props: UserProps) {
    this._id = props.id;
    this._name = props.name;
    this._gatewayId = props.gatewayId;
    this._paymentBalance = props.paymentBalance;
    this._walletBalance = props.walletBalance;
    this._createdAt = props.createdAt;
    this._updatedAt = props.updatedAt;
  }

  // Getter
  get id() {
    return this._id;
  }
  get name() {
    return this._name;
  }
  get gatewayId() {
    return this._gatewayId;
  }
  get paymentBalance() {
    return this._paymentBalance;
  }
  get walletBalance() {
    return this._walletBalance;
  }
  get createdAt() {
    return this._createdAt;
  }
  get updatedAt() {
    return this._updatedAt;
  }

  // 비즈니스 규칙: 결제 금액 검증
  static validatePaymentAmount(amount: number): void {
    if (amount <= 0) {
      throw new Error("결제 금액은 0원보다 커야 합니다");
    }
  }

  // 비즈니스 규칙: 환불 가능 여부
  canRefund(amount: number, minRefundAmount: number): boolean {
    return this._paymentBalance >= amount && amount >= minRefundAmount;
  }

  // 비즈니스 규칙: 지갑 잔액 사용 가능 여부
  canUseWallet(amount: number): boolean {
    return this._walletBalance >= amount;
  }
}
```

**다시 강조**: `canRefund`, `canUseWallet` 같은 메서드는 **boolean만 반환**한다. "환불 가능", "환불 불가" 같은 메시지는 Adapter에서 처리한다.

### 잔액 필드 상세 설명

`paymentBalance`는 환불 가능한 금액, `walletBalance`는 환불받아 적립된 예치금이다. 10,000원 결제 → 5,000원 환불하면, `paymentBalance`: 5,000원 (차감), `walletBalance`: 5,000원 (적립)으로 **현금 결제 금액**과 **예치금**을 구분한다.

---

**PaymentTransaction Entity:**

결제 거래 내역을 관리하는 Entity이다.

```ts
// domain/entities/PaymentTransaction.ts
type TransactionType = "payment" | "refund"; // payment: 결제, refund: 환불

interface PaymentTransactionProps {
  id: string;
  userId: number;
  gatewayId: number;
  type: TransactionType;
  amount: number;
  balanceAfter: number;
  createdAt: Date;
}

export class PaymentTransaction {
  private readonly _id: string; // 거래 고유 ID (UUID, 중복 방지용 식별자)
  private readonly _userId: number; // 거래를 수행한 사용자 ID
  private readonly _gatewayId: number; // 거래를 처리한 결제 대행사 ID
  private readonly _type: TransactionType; // 거래 유형 (payment: 결제, refund: 환불)
  private readonly _amount: number; // 거래 금액
  private readonly _balanceAfter: number; // 거래 후 잔액 (이 거래 이후 남은 사용자 잔액)
  private readonly _createdAt: Date; // 거래 발생 시각

  constructor(props: PaymentTransactionProps) {
    this._id = props.id;
    this._userId = props.userId;
    this._gatewayId = props.gatewayId;
    this._type = props.type;
    this._amount = props.amount;
    this._balanceAfter = props.balanceAfter;
    this._createdAt = props.createdAt;
  }

  // Getter들
  get id(): string {
    return this._id; // 거래 조회, 영수증 출력 등에 사용
  }

  get userId(): number {
    return this._userId; // 사용자별 거래 내역 필터링에 사용
  }

  get gatewayId(): number {
    return this._gatewayId; // PG사별 거래 통계, 필터링에 사용
  }

  get type(): TransactionType {
    return this._type; // 거래 유형 표시 (결제/환불 아이콘, 색상 구분 등)
  }

  get amount(): number {
    return this._amount; // 거래 금액 표시, 통계 계산에 사용
  }

  get balanceAfter(): number {
    return this._balanceAfter; // 거래 후 잔액 표시, 잔액 변화 추적에 사용
  }

  get createdAt(): Date {
    return this._createdAt; // 거래 발생 시각 표시, 거래 내역 시간순 정렬에 사용
  }

  // 비즈니스 규칙: 결제 거래 생성
  static createPaymentTransaction(
    userId: number,
    gatewayId: number,
    amount: number,
    balanceAfter: number
  ): PaymentTransaction {
    if (amount <= 0) {
      throw new Error("결제 금액은 0원보다 커야 합니다");
    }

    return new PaymentTransaction({
      id: crypto.randomUUID(), // 고유 ID 생성
      userId,
      gatewayId,
      type: "payment",
      amount,
      balanceAfter,
      createdAt: new Date(),
    });
  }

  // 비즈니스 규칙: 환불 거래 생성
  static createRefundTransaction(
    userId: number,
    gatewayId: number,
    amount: number,
    balanceAfter: number
  ): PaymentTransaction {
    if (amount <= 0) {
      throw new Error("환불 금액은 0원보다 커야 합니다");
    }

    return new PaymentTransaction({
      id: crypto.randomUUID(),
      userId,
      gatewayId,
      type: "refund",
      amount,
      balanceAfter,
      createdAt: new Date(),
    });
  }
}
```

---

Entity들은 다음과 같은 `비즈니스 규칙(검증 로직)`을 담는다:

**PaymentGateway Entity:**

- **최소 결제 금액**: 100원 이상인지 검증
- **예치금 잔액 확인**: 결제 가능 여부 검증 (상태 변경 없음)
- **충전 금액 검증**: 0원 초과인지 검증

**User Entity:**

- **결제 금액 검증**: 0원 초과인지 검증
- **환불 가능 여부**: 잔액 충분 여부 및 최소 금액 검증 (상태 변경 없음)
- **지갑 사용 가능 여부**: 지갑 잔액 충분 여부 검증 (상태 변경 없음)

**PaymentTransaction Entity:**

- **거래 내역 생성**: 결제/환불 거래 기록 생성 (factory method)
- **금액 검증**: 거래 금액이 0보다 큰지 확인
- **거래 타입**: "process"(결제)와 "convert"(환불) 구분
- **완전 불변성**: 모든 속성이 readonly, 한 번 생성된 거래는 수정 불가

기획자가 "최소 결제 금액을 2,000원으로 바꿔주세요"라고 하면? `MIN_PAYMENT_AMOUNT` 한 줄만 바꾸면 된다.

```diff
// domain/entities/PaymentGateway.ts
- static readonly MIN_PAYMENT_AMOUNT = 1_000;
+ static readonly MIN_PAYMENT_AMOUNT = 2_000;
```

컴포넌트는 건드릴 필요가 없다.

---

### Entity는 왜 불변인가?

우리 프로젝트는 **서버 상태 관리를 Tanstack Query에게 위임**했다. 만약 Entity class에서도 상태를 관리하면 어떻게 될까?

```ts
// ❌ Entity가 상태를 직접 변경하면?
class User {
  private _balance: number;

  deposit(amount: number) {
    this._balance += amount; // Entity가 상태 변경
  }
}

// 문제: Tanstack Query 캐시와 Entity 상태가 따로 논다!
// - Entity: _balance = 10000
// - Tanstack Query 캐시: balance = 5000
// 어느 것이 진짜인가?
```

Entity와 Tanstack Query 캐시가 **각자 상태를 관리**하면 불일치가 발생한다. 따라서 Entity는 `readonly`로 만들어 **검증만 담당**하고, 상태는 Tanstack Query가 서버 응답으로만 업데이트한다.

```ts
// ✅ 프론트엔드 Entity: readonly + 검증 메서드만
class User {
  private readonly _balance: number;

  canDeposit(amount: number): boolean {
    return amount > 0; // 검증만
  }
}

// UseCase가 서버에 요청 → Tanstack Query가 캐시 업데이트
const updatedUser = await userRepository.deposit(user.id, amount);
```

백엔드는 Entity가 상태를 직접 변경하고 DB에 저장하지만, 프론트엔드는 Tanstack Query가 서버 응답을 통해서만 상태를 업데이트한다.
| **상태 관리** | `ORM` | `TanStack Query` |

---

### Repository Interface: 데이터 접근 추상화

`Domain` Layer는 데이터가 **어디서 오는지** 모른다. HTTP API인지, LocalStorage인지, IndexedDB인지 알 필요가 없다. 단지 "**어떤 데이터가 필요한지**"만 정의한다.

이것이 바로 `Repository Interface`다. `Domain`은 인터페이스만 정의하고, `Data` Layer가 실제 구현을 제공한다.

```ts
// domain/repositories/PaymentGatewayRepository.ts
import type { PaymentGateway } from "@/domain/entities/PaymentGateway";
import type { User } from "@/domain/entities/User";

export interface PaymentGatewayRepository {
  getPaymentGateway(gatewayId: number): Promise<PaymentGateway>; // Entity 반환
  processPayment(
    gatewayId: number,
    userId: number,
    paymentAmount: number
  ): Promise<User>; // Entity 반환
}
```

```ts
// domain/repositories/UserRepository.ts
import type { User } from "@/domain/entities/User";

export interface UserRepository {
  getUser(userId: number): Promise<User>;
  processPayment(userId: number, paymentAmount: number): Promise<User>;
  processRefund(userId: number, refundAmount: number): Promise<User>;
}
```

**핵심은 반환 타입**이다. `Repository Interface`는 `DTO`가 아닌 `Entity`를 반환한다. `Domain`은 `DTO`의 존재조차 모른다.

**레이어별 역할**:
- **Domain**: 인터페이스만 정의 (`domain/repositories/`)
- **Data**: 인터페이스를 구현 (`data/repositories/`)

```ts
// Domain은 "무엇이 필요한지"만 정의
// domain/repositories/PaymentGatewayRepository.ts
export interface PaymentGatewayRepository {
  getPaymentGateway(id: number): Promise<PaymentGateway>;
}

// Data는 "어떻게 가져올지" 구현
// data/repositories/PaymentGatewayRepositoryImpl.ts
export class PaymentGatewayRepositoryImpl implements PaymentGatewayRepository {
  constructor(private dataSource: PaymentGatewayDataSource) {}

  async getPaymentGateway(id: number): Promise<PaymentGateway> {
    const dto = await this.dataSource.getPaymentGateway(id);
    return mapDtoToEntity(dto); // DTO → Entity 변환
  }
}
```

이렇게 하면:

- **`Domain`은 `Data`를 몰라도 된다**: `의존성 역전 원칙`
- **테스트가 쉬워진다**: Mock Repository를 주입하면 끝
- **데이터 소스 교체가 자유롭다**: HTTP → LocalStorage로 바꿔도 `Domain`은 모른다

마치 콘센트와 플러그의 관계다. 콘센트(Interface)는 규격만 정의하고, 어떤 전기(데이터)가 들어오든 상관없다.

---

### UseCase: 비즈니스 로직의 흐름

`UseCase`는 여러 `Entity`를 조합해서 **비즈니스 로직**을 수행한다.

```ts
// domain/useCases/processPayment.ts
import { PaymentGateway } from "@/domain/entities/PaymentGateway";
import { User } from "@/domain/entities/User";
import type { PaymentGatewayRepository } from "@/domain/repositories/PaymentGatewayRepository";
import type { UserRepository } from "@/domain/repositories/UserRepository";

export class ProcessPaymentUseCase {
  constructor(
    private paymentGatewayRepository: PaymentGatewayRepository,
    private userRepository: UserRepository
  ) {}

  async execute(
    gateway: PaymentGateway,
    userId: number,
    paymentAmount: number
  ): Promise<User> {
    // 1. 입력값 검증
    PaymentGateway.validatePaymentAmount(paymentAmount);

    if (!gateway.canProcessPayment(paymentAmount)) {
      throw new Error("예치금 잔액이 부족합니다");
    }

    // 2. Repository 호출 (서버가 상태 변경)
    const user = await this.userRepository.processPayment(
      userId,
      paymentAmount
    );

    return user;
  }
}
```

`ProcessPaymentUseCase`는 다음을 수행한다:

1. **입력값 검증**: `PaymentGateway` Entity의 규칙을 사용해 결제 가능 여부를 확인한다
2. **Repository 호출**: `userRepository.processPayment()`로 서버에 결제 요청을 보내고, 서버가 변경한 최신 상태를 `Entity`로 반환받는다

**중요한 원칙**:

- **`Entity`는 불변이다**: Tanstack Query가 서버 상태를 관리하므로, Entity는 검증만 담당 (상태 중복 방지)
- **`UseCase`는 `Entity`만 받는다**: `DTO`를 볼 일이 없다
- **Tanstack Query가 상태를 관리한다**: Entity와 캐시 사이의 불일치를 방지한다

`UseCase`는 **어떤 UI**를 쓰는지, **어떤 HTTP 클라이언트**를 쓰는지 전혀 모른다. 오직 비즈니스 로직만 담당한다.

### 환불 처리 UseCase 예시

결제 금액을 지갑 잔액으로 환불하는 로직도 `UseCase`로 만들 수 있다.

```ts
// domain/useCases/processRefund.ts
import { User } from "@/domain/entities/User";
import { PaymentGateway } from "@/domain/entities/PaymentGateway";
import type { UserRepository } from "@/domain/repositories/UserRepository";

interface ProcessRefundConfig {
  minConversionAmount: number; // 최소 환불 금액 (설정 가능)
}

export class ProcessRefundUseCase {
  constructor(
    private userRepository: UserRepository,
    private config: ProcessRefundConfig
  ) {}

  async execute(
    user: User,
    gateway: PaymentGateway,
    refundAmount: number
  ): Promise<User> {
    // 1. 입력값 검증
    if (!user.canRefund(refundAmount, this.config.minConversionAmount)) {
      throw new Error("결제 잔액이 부족하거나 최소 환불 금액 미만입니다");
    }

    if (!gateway.canProcessPayment(refundAmount)) {
      throw new Error("결제 대행사 예치금이 부족합니다");
    }

    // 2. Repository 호출 (서버가 상태 변경)
    const updatedUser = await this.userRepository.processRefund(
      user.id,
      refundAmount
    );

    return updatedUser;
  }
}
```

환불 처리 로직도 **비즈니스 규칙**(환불 가능 여부, 최소 금액)과 **데이터 처리**(서버 요청)를 분리했다. 최소 환불 금액은 설정으로 주입받아 유연하게 변경할 수 있다.

---

## DI Layer: DI Container로 의존성 관리

지금까지 `Entity`, `Repository`, `UseCase`를 만들었다. 하지만 이 모든 의존성을 어떻게 관리할까?

`UseCase`는 `Repository`가 필요하고, `Repository`는 `DataSource`가 필요하고, `DataSource`는 `HttpClient`가 필요하다. 매번 수동으로 생성하면 코드가 복잡해진다.

이때 **DI Container**를 사용한다. DI Container는 **의존성을 한 곳에서 관리하고, 필요할 때 주입**해준다.

마치 공장에서 제품을 만들 때 필요한 부품을 미리 준비해두는 것과 같다.

```ts
// di/DIContainer.ts
import { HttpClient } from "@/infrastructure/http/HttpClient";
import { PaymentGatewayDataSource } from "@/data/datasources/paymentGatewayDataSource";
import { UserDataSource } from "@/data/datasources/userDataSource";
import { PaymentGatewayRepositoryImpl } from "@/data/repositories/PaymentGatewayRepositoryImpl";
import { UserRepositoryImpl } from "@/data/repositories/UserRepositoryImpl";
import { ProcessPaymentUseCase } from "@/domain/useCases/processPayment";
import { ProcessRefundUseCase } from "@/domain/useCases/processRefund";
import type { PaymentGatewayRepository } from "@/domain/repositories/PaymentGatewayRepository";
import type { UserRepository } from "@/domain/repositories/UserRepository";

class DIContainer {
  private static _httpClient: HttpClient;
  private static _paymentGatewayDataSource: PaymentGatewayDataSource;
  private static _userDataSource: UserDataSource;
  private static _paymentGatewayRepository: PaymentGatewayRepository;
  private static _userRepository: UserRepository;

  static get httpClient() {
    return (this._httpClient ??= new AxiosHttpClient({
      baseURL: import.meta.env.VITE_API_URL,
      timeout: 10_000,
    }));
  }

  static get paymentGatewayDataSource() {
    return (this._paymentGatewayDataSource ??= new PaymentGatewayDataSource(
      this.httpClient
    ));
  }

  static get userDataSource() {
    return (this._userDataSource ??= new UserDataSource(this.httpClient));
  }

  static get paymentGatewayRepository() {
    return (this._paymentGatewayRepository ??= new PaymentGatewayRepositoryImpl(
      this.paymentGatewayDataSource
    ));
  }

  static get userRepository() {
    return (this._userRepository ??= new UserRepositoryImpl(
      this.userDataSource
    ));
  }

  // UseCases (매번 새로 생성)
  static get processPaymentUseCase() {
    return new ProcessPaymentUseCase(
      this.paymentGatewayRepository,
      this.userRepository
    );
  }

  static get processRefundUseCase() {
    return new ProcessRefundUseCase(this.userRepository, {
      minConversionAmount: 1_000,
    });
  }

  // 테스트용
  static setMockRepositories(pgRepo, userRepo) {
    this._paymentGatewayRepository = pgRepo;
    this._userRepository = userRepo;
  }
}

export default DIContainer;
```

**전략**: `HttpClient`, `DataSource`, `Repository`는 싱글톤 (`??=`), `UseCase`는 매번 새로 생성, 테스트 시 Mock 주입 가능.

### DI 라이브러리 활용하기

위 방식은 초기 프로젝트에 적합하다. 하지만 프로젝트가 커지면 DIContainer 파일이 비대해지고 관리가 어려워진다.

서버 프레임워크(NestJS, Spring)나 모바일 프레임워크(Flutter, Swift, Android)는 이 문제를 해결하기 위해 DI 라이브러리를 적극 활용한다. 프론트엔드에서도 마찬가지로 DI 라이브러리를 도입하면 의존성 관리가 훨씬 쉬워진다.

**대표적인 DI 라이브러리** (2025년 11월 기준):
- **[Awilix](https://github.com/jeffijoe/awilix)** - 간단하고 직관적인 IoC 컨테이너 (11.3k stars)
- **[InversifyJS](https://github.com/inversify/InversifyJS)** - 데코레이터 기반 DI (11.2k stars)
- **[TypeDI](https://github.com/typestack/typedi)** - 심플하고 강력한 DI (4.3k stars)
- **[TSyringe](https://github.com/microsoft/tsyringe)** - Microsoft의 경량 DI (5.3k stars)

이 중 Awilix를 사용한 예시를 살펴보자.

#### Before: 수동 DI Container

```ts
// di/DIContainer.ts
class DIContainer {
  private static _httpClient?: HttpClient;
  private static _paymentGatewayDataSource?: PaymentGatewayDataSource;
  private static _paymentGatewayRepository?: PaymentGatewayRepository;

  static get httpClient() {
    return (this._httpClient ??= new AxiosHttpClient());
  }

  static get paymentGatewayDataSource() {
    return (this._paymentGatewayDataSource ??= new PaymentGatewayDataSourceImpl(
      this.httpClient
    ));
  }

  static get paymentGatewayRepository() {
    return (this._paymentGatewayRepository ??= new PaymentGatewayRepositoryImpl(
      this.paymentGatewayDataSource
    ));
  }

  static get processPaymentUseCase() {
    return new ProcessPaymentUseCase(
      this.paymentGatewayRepository,
      this.userRepository
    );
  }

  // 의존성이 늘어날수록 코드가 길어진다...
}
```

**문제점**:
- 의존성이 추가될 때마다 getter 메서드를 직접 작성해야 한다
- 생명주기 관리(싱글톤, 매번 생성)를 수동으로 처리해야 한다
- 테스트 시 Mock 교체가 번거롭다

#### After: Awilix 사용

```ts
// di/container.ts
import { createContainer, asClass, InjectionMode } from 'awilix';

export const container = createContainer({
  injectionMode: InjectionMode.PROXY,
});

// 의존성 등록 (싱글톤)
container.register({
  // Infrastructure
  httpClient: asClass(AxiosHttpClient).singleton(),

  // DataSource
  paymentGatewayDataSource: asClass(PaymentGatewayDataSourceImpl).singleton(),
  userDataSource: asClass(UserDataSourceImpl).singleton(),

  // Repository
  paymentGatewayRepository: asClass(PaymentGatewayRepositoryImpl).singleton(),
  userRepository: asClass(UserRepositoryImpl).singleton(),

  // UseCase (매번 새로 생성)
  processPaymentUseCase: asClass(ProcessPaymentUseCase).transient(),
  processRefundUseCase: asClass(ProcessRefundUseCase).transient(),
});

// 사용
const useCase = container.cradle.processPaymentUseCase;
```

**개선점**:
- ✅ **선언적 등록**: `asClass().singleton()`으로 생명주기를 명시적으로 관리
- ✅ **자동 주입**: 생성자 파라미터 이름으로 자동 의존성 해결
- ✅ **테스트 용이성**: Mock 교체가 간단해진다

#### 테스트 시 Mock 교체

```ts
// presentation/hooks/useProcessPayment.test.ts
import { container } from '@/di/container';

describe('useProcessPayment', () => {
  beforeEach(() => {
    // Mock Repository로 교체
    container.register({
      paymentGatewayRepository: asValue(mockPaymentGatewayRepository),
      userRepository: asValue(mockUserRepository),
    });
  });

  afterEach(() => {
    container.dispose(); // 테스트 후 정리
  });

  it('결제 프로세스가 정상 동작한다', async () => {
    const { result } = renderHook(() => useProcessPayment());
    // ... 테스트 로직
  });
});
```

**테스트 개선점**:
- Mock 교체: `container.register()`로 간단하게 교체
- 테스트 격리: `container.dispose()`로 각 테스트 독립성 보장
- 설정 재사용: 여러 테스트에서 동일한 Mock 설정 공유 가능

프로젝트 규모가 커지고 의존성 관리가 복잡해진다면, DI 라이브러리 도입을 고려해보자.

### 왜 싱글톤인가? 메모리 낭비 방지

DI Container 없이 매번 인스턴스를 생성하면 어떻게 될까?

```ts
// ❌ 안티패턴: 컴포넌트마다 인스턴스 생성
function useProcessPayment() {
  const httpClient = new AxiosHttpClient({ ... });  // 인스턴스 1
  const dataSource = new PaymentDataSource(httpClient);
  const repository = new PaymentRepositoryImpl(dataSource);
  const useCase = new ProcessPaymentUseCase(repository);
  // ...
}

function useProcessRefund() {
  const httpClient = new AxiosHttpClient({ ... });  // 인스턴스 2 (중복!)
  const dataSource = new UserDataSource(httpClient);
  const repository = new UserRepositoryImpl(dataSource);
  const useCase = new ProcessRefundUseCase(repository);
  // ...
}

// 문제: HttpClient가 컴포넌트마다 생성됨
// - 10개 컴포넌트 → HttpClient 10개
// - 메모리 낭비 + 설정 불일치 가능
```

DI Container는 `??=` (Nullish coalescing assignment)로 **싱글톤**을 구현한다:

```ts
// ✅ DI Container: 싱글톤으로 1번만 생성
static get httpClient() {
  return (this._httpClient ??= new AxiosHttpClient({ ... }));
  // _httpClient가 없으면 생성, 있으면 기존 것 재사용
}
```

**이점**:

- **메모리 효율**: `HttpClient` 1개만 생성, 모든 곳에서 재사용
- **설정 일관성**: `baseURL`, `timeout` 등 설정이 항상 동일
- **성능**: 인스턴스 생성 비용 절감

마치 사무실에서 복사기 1대를 여러 사람이 공유하는 것과 같다. 각자 복사기를 사는 것보다 훨씬 효율적이다.

---

## Presentation Layer: UI와 상호작용

`Presentation` 레이어는 사용자와 직접 상호작용한다. 2가지로 구성된다:

1. `Hook`: `Domain`의 `UseCase`를 호출
2. `Component`: UI를 렌더링

### Hook: UseCase 호출

```ts
// presentation/hooks/useProcessPayment.ts
import { useMutation } from "@tanstack/react-query";
import DIContainer from "@/di/DIContainer";
import type { PaymentGateway } from "@/domain/entities/PaymentGateway";
import type { User } from "@/domain/entities/User";

interface ProcessPaymentParams {
  userId: number;
  amount: number;
}

export function useProcessPayment(paymentGateway: PaymentGateway) {
  return useMutation<User, Error, ProcessPaymentParams>({
    mutationFn: async ({ userId, amount }: ProcessPaymentParams) => {
      // DI Container에서 UseCase를 가져온다
      const processPaymentUseCase = DIContainer.processPaymentUseCase;
      return await processPaymentUseCase.execute(
        paymentGateway,
        userId,
        amount
      );
    },
  });
}
```

`useProcessPayment` Hook은 `DI Container`에서 `UseCase`를 가져온다. 의존성을 외부에서 주입받는 것이 아니라, **중앙 집중식으로 관리**한다.

### Component: UI 렌더링

```ts
// presentation/components/PaymentButton.tsx
import { useProcessPayment } from "@/presentation/hooks/useProcessPayment";
import type { PaymentGateway } from "@/domain/entities/PaymentGateway";

interface PaymentButtonProps {
  paymentGateway: PaymentGateway;
  userId: number;
  paymentAmount: number;
}

export function PaymentButton({
  paymentGateway,
  userId,
  paymentAmount,
}: PaymentButtonProps) {
  const { mutate, isPending, error } = useProcessPayment(paymentGateway);

  const handleClick = () => {
    mutate(
      { userId, amount: paymentAmount },
      {
        onSuccess: (user) => {
          alert(`결제 완료! 사용자: ${user.name}`);
        },
      }
    );
  };

  return (
    <div>
      <button onClick={handleClick} disabled={isPending}>
        {isPending ? "결제 중..." : "결제하기"}
      </button>
      {error && <p style={{ color: "red" }}>{error.message}</p>}
    </div>
  );
}
```

`PaymentButton` 컴포넌트는 **UI만** 담당한다. 비즈니스 로직은 `UseCase`에 위임하고, `UseCase`는 `DI Container`가 제공한다.

### 의존성 주입

"어? `UseCase`를 어디서 가져왔지?"

앞에서 만든 **DI Container**에서 가져온다! Hook에서 `DIContainer.processPaymentUseCase`를 호출하면, 모든 의존성이 자동으로 주입된 `UseCase`를 받을 수 있다.

**왜 DI Container가 중요할까?**

컴포넌트 내부에서 직접 생성하면 **테스트가 매우 어렵다**. 테스트하려면 실제 `HttpClient`를 모킹하거나, 테스트용 서버를 띄워야 한다.

```ts
// ❌ 나쁜 예시: 컴포넌트 내부에서 생성
function PaymentButton({ paymentGateway, userId, paymentAmount }: Props) {
  // 컴포넌트 안에서 직접 생성
  const httpClient = new AxiosHttpClient();
  const paymentGatewayRepo = new PaymentGatewayRepositoryImpl(httpClient);
  const userRepo = new UserRepositoryImpl(httpClient);
  const useCase = new ProcessPaymentUseCase(paymentGatewayRepo, userRepo);

  const handleProcessPayment = async () => {
    await useCase.execute(paymentGateway, userId, paymentAmount);
  };
  // ...
}

// 문제점:
// 1. 실제 API를 호출함 (테스트가 느리고 불안정)
// 2. Mock으로 바꿀 방법이 없음
// 3. 네트워크 없으면 테스트 실패
```

DI Container를 사용하면 **중앙 집중식으로 의존성을 관리**하고, **테스트 시 Mock으로 교체**할 수 있다.

#### 테스트에서 Mock 주입

```ts
// presentation/components/PaymentButton.test.tsx
import { test, expect, vi, beforeEach, afterEach } from "vitest";
import { render, fireEvent, screen } from "@testing-library/react";
import { PaymentButton } from "./PaymentButton";
import DIContainer from "@/di/DIContainer";
import type { PaymentGatewayRepository } from "@/domain/repositories/PaymentGatewayRepository";
import type { UserRepository } from "@/domain/repositories/UserRepository";
import { PaymentGateway } from "@/domain/entities/PaymentGateway";
import { User } from "@/domain/entities/User";

// Mock Repository 생성
const mockPaymentGatewayRepository: PaymentGatewayRepository = {
  getPaymentGateway: vi.fn(),
  processPayment: vi.fn(),
};

const mockUserRepository: UserRepository = {
  getUser: vi.fn(),
  processPayment: vi.fn().mockResolvedValue(
    new User({
      id: 1,
      name: "김사용자",
      gatewayId: 1,
      paymentBalance: 1_500, // 결제 후 잔액
      walletBalance: 0,
      createdAt: new Date(),
      updatedAt: new Date(),
    })
  ),
  processRefund: vi.fn(),
};

beforeEach(() => {
  // 테스트 전에 Mock Repository 주입
  DIContainer.setMockRepositories(
    mockPaymentGatewayRepository,
    mockUserRepository
  );
});

afterEach(() => {
  // 테스트 후 DI Container 초기화
  DIContainer.reset();
  vi.clearAllMocks();
});

test("결제 버튼 클릭 시 UseCase 호출", async () => {
  const paymentGateway = new PaymentGateway({
    id: 1,
    name: "테스트결제사",
    walletBalance: 100_000,
    createdAt: new Date(),
    updatedAt: new Date(),
  });

  render(
    <PaymentButton
      paymentGateway={paymentGateway}
      userId={1}
      paymentAmount={500}
    />
  );

  fireEvent.click(screen.getByText("결제하기"));

  // Mock Repository가 올바르게 호출되었는지 검증
  await vi.waitFor(() => {
    expect(mockUserRepository.processPayment).toHaveBeenCalledWith(1, 1_000);
  });
});
```

DI Container 덕분에 **실제 API를 호출하지 않고도** 컴포넌트를 테스트할 수 있다. `setMockRepositories`로 Mock을 주입하고, `reset()`으로 초기화하면 된다.

### Adapter: Entity를 UI에 맞게 가공 (선택)

대부분의 경우 Entity를 그대로 컴포넌트에서 사용하면 된다. 하지만 **복잡한 UI 로직**이나 **여러 Entity를 조합**해야 할 때는 `Adapter`를 사용한다.

`Adapter`는 Entity를 UI에 최적화된 형태로 변환한다. 마치 콘센트 어댑터처럼, Entity(220V)를 UI(110V)에 맞게 변환하는 역할이다.

**Entity vs Adapter 역할 구분**:
- **Entity**: 비즈니스 규칙과 판단 (boolean 반환)
  ```ts
  user.canRefund(amount, minAmount) // → true/false
  ```
- **Adapter**: UI 표현과 포맷팅 (텍스트, 색상, 조합)
  ```ts
  canProcessRefund: user.canRefund(...), // → boolean
  minConversionMessage: "최소 10,000원 이상", // → 텍스트
  displayBalance: "500P" // → 포맷팅
  ```

Adapter는 Mapper와 마찬가지로 class가 아닌 **순수 함수**로 구현한다.

#### 공통 유틸 함수: formatters.ts

여러 Adapter에서 반복 사용되는 포맷팅 로직은 공통 유틸 함수로 분리한다.

```ts
// presentation/utils/formatters.ts

/**
 * 결제 금액을 포맷팅
 * @example formatPayments(500) // "500P"
 */
export function formatPayments(payments: number): string {
  return `${payments.toLocaleString()}P`;
}

/**
 * 날짜를 한국 형식으로 포맷팅
 * @example formatDate(new Date()) // "2025-01-08"
 */
export function formatDate(date: Date): string {
  return date.toLocaleDateString("ko-KR");
}

/**
 * 날짜시간을 한국 형식으로 포맷팅
 * @example formatDateTime(new Date()) // "2025-01-08 15:30"
 */
export function formatDateTime(date: Date): string {
  return date.toLocaleString("ko-KR", {
    year: "numeric",
    month: "2-digit",
    day: "2-digit",
    hour: "2-digit",
    minute: "2-digit",
  });
}
```

#### 언제 Adapter를 쓸까?

다음과 같은 경우에만 `Adapter`를 사용한다:

1. **여러 Entity를 조합**해야 할 때 (User + PaymentGateway)
2. **복잡한 UI 변환**이 필요할 때 (숫자 → "500P", 날짜 → "2025-01-08")
3. **Entity를 순수하게 유지**하고 싶을 때 (UI 로직을 Entity에 넣지 않기)

#### Adapter의 3가지 핵심 장점

##### 1. UI 로직 격리

Entity는 **비즈니스 규칙**만 담당하고, UI 표현 로직은 `Adapter`가 담당한다.

```ts
// ❌ Entity에 UI 로직을 넣으면?
class User {
  getDisplayPayments(): string {
    return `${this.paymentBalance.toLocaleString()}P`; // UI 로직이 Entity에!
  }

  getDisplayWallet(): string {
    return `${this.walletBalance.toLocaleString()}원`; // UI 로직이 Entity에!
  }
}

// ✅ Adapter 함수로 분리
export function adaptUserForView(user: User) {
  return {
    displayPaymentBalance: formatPayments(user.paymentBalance), // UI 로직은 Adapter에
    displayWalletBalance: formatCurrency(user.walletBalance),
  };
}
```

이렇게 분리하면 여러 컴포넌트에서 동일한 Adapter 함수를 재사용할 수 있다.

##### 2. Entity 순수성 유지

Entity는 UI를 전혀 모른다. 덕분에 **서버와 프론트엔드에서 동일한 Entity**를 사용할 수 있다.

```ts
// domain/entities/User.ts (프론트엔드 - 불변 패턴)
export class User {
  private readonly _paymentBalance: number; // 불변
  private readonly _walletBalance: number; // 불변

  // ✅ 비즈니스 규칙만 담당 (검증 로직)
  canRefund(amount: number, minRefundAmount: number): boolean {
    return this._paymentBalance >= amount && amount >= minRefundAmount;
  }

  canUseWallet(amount: number): boolean {
    return this._walletBalance >= amount;
  }

  // ❌ 상태 변경 메서드는 없음 (서버가 상태 변경)
  // processRefundToBalance() 같은 메서드 없음
}
```

##### 3. 테스트 용이성

Adapter는 **순수 함수**라서 테스트가 쉽다. 같은 입력을 넣으면 항상 같은 결과가 나오고, 외부 상태를 변경하지 않기 때문이다.

```ts
import { test, expect } from "vitest";
import { adaptUserForView } from "@/presentation/adapters/userAdapter";

test("userAdapter는 결제 금액과 지갑 잔액을 포맷팅한다", () => {
  const user = new User({
    id: 1,
    name: "김사용자",
    gatewayId: 1,
    paymentBalance: 5_000,
    walletBalance: 10_000,
    createdAt: new Date(),
    updatedAt: new Date(),
  });

  const view = adaptUserForView(user, paymentGateway, 5_000);

  expect(view.displayPaymentBalance).toBe("5,000P");
  expect(view.displayWalletBalance).toBe("10,000원");
});
```

#### 예시 1: 복잡한 UI 변환 - userAdapter

사용자 정보를 UI에 맞게 가공하는 Adapter 함수를 만들어보자.

```ts
// presentation/adapters/userAdapter.ts
import type { User } from "@/domain/entities/User";
import type { PaymentGateway } from "@/domain/entities/PaymentGateway";
import {
  formatCurrency,
  formatPayments,
  formatDate,
} from "@/presentation/utils/formatters";

interface UserView {
  userId: number;
  userName: string;
  paymentGatewayName: string;
  displayPaymentBalance: string; // "5,000P"
  displayWalletBalance: string; // "10,000원"
  canProcessRefund: boolean;
  minConversionMessage: string; // "최소 5,000원부터 전환 가능"
  lastUpdated: string; // "2025-01-08"
}

export function adaptUserForView(
  user: User,
  paymentGateway: PaymentGateway,
  minConversionAmount: number
): UserView {
  return {
    userId: user.id,
    userName: user.name,
    paymentGatewayName: paymentGateway.name,
    displayPaymentBalance: formatPayments(user.paymentBalance),
    displayWalletBalance: formatCurrency(user.walletBalance),
    canProcessRefund: user.canRefund(user.paymentBalance, minConversionAmount),
    minConversionMessage: getConversionMessage(minConversionAmount),
    lastUpdated: formatDate(user.updatedAt),
  };
}

function getConversionMessage(minAmount: number): string {
  return `최소 ${formatCurrency(minAmount)}부터 전환 가능`;
}
```

#### 컴포넌트에서 사용하기

컴포넌트는 Adapter로 변환된 데이터를 사용한다.

```ts
// presentation/components/UserCard.tsx
export function UserCard({ user, paymentGateway, minConversionAmount }: Props) {
  const view = adaptUserForView(user, paymentGateway, minConversionAmount);

  return (
    <div className="user-card">
      <h3>{view.userName}</h3>
      <p className="payments">{view.displayPaymentBalance}</p>
      <p className="wallet">{view.displayWalletBalance}</p>
    </div>
  );
}
```

#### Adapter를 쓰지 않는 경우

단순한 경우에는 Entity를 그대로 쓴다.

```ts
// presentation/components/SimpleRefundButton.tsx
import type { User } from "@/domain/entities/User";

interface Props {
  user: User;
  minConversionAmount: number;
}

export function SimpleRefundButton({ user, minConversionAmount }: Props) {
  // Adapter 없이 Entity 직접 사용 (간단할 때)
  return (
    <button
      disabled={!user.canRefund(user.paymentBalance, minConversionAmount)}
    >
      {user.paymentBalance}P 환불하기
    </button>
  );
}
```

---

## 전체 결제 처리 Flow: 모든 레이어 통과하기

이제 관리자가 "결제 처리" 버튼을 클릭했을 때 **모든 레이어**를 어떻게 통과하는지 살펴보자.

### 1. 관리자가 버튼 클릭

```ts
// Presentation Layer: Component
<button onClick={handleClick}>결제 처리</button>
```

### 2. Hook이 UseCase 호출

```ts
// Presentation Layer: Hook
const transaction = await processPaymentUseCase.execute(
  gatewayId,
  userId,
  paymentAmount
);
```

### 3. UseCase가 비즈니스 로직 실행

```ts
// Domain Layer: UseCase
PaymentGateway.validatePaymentAmount(amount); // 결제 금액 검증
gateway.canProcessPayment(amount); // 예치금 잔액 검증
const transactionDto = await this.paymentRepository.processPayment(
  gatewayId,
  userId,
  amount
); // API 호출
```

### 4. Repository가 DataSource 호출

```ts
// Data Layer: Repository
return this.paymentDataSource.processPayment(gatewayId, userId, amount);
```

### 5. DataSource가 API 호출

```ts
// Data Layer: DataSource
return this.httpClient.post<PaymentTransactionDto>("/api/payments/process", {
  gateway_id: gatewayId,
  user_id: userId,
  amount,
});
```

### 6. HTTP Client가 실제 요청

```ts
// Infrastructure Layer: HTTP Client
const response = await axios.post(url, data);
return response.data;
```

### 7. 응답이 다시 역순으로 돌아옴

```
Infrastructure → Data (DataSource) → Data (Repository) → Data (Mapper) → Domain (Entity) → Presentation
```

```ts
// Data Layer: Mapper 함수
const transaction = mapPaymentTransactionDtoToDomain(transactionDto);
```

### 7. Entity가 컴포넌트로 전달

```ts
// Presentation Layer: Component
alert(`결제 처리 완료! 거래 ID: ${transaction.id}`);
```

### 전체 Flow 요약

```
👤 관리자 버튼 클릭
  ↓
🎨 Presentation: Hook → UseCase 호출
  ↓
🧠 Domain: UseCase → 비즈니스 로직 + Repository 호출
  ↓
📦 Data: Repository → HTTP Client 호출
  ↓
🌐 Infrastructure: HTTP Client → API 요청
  ↓
🌐 Infrastructure: 응답 받음 (DTO)
  ↓
📦 Data: Mapper 함수 → DTO를 Entity로 변환
  ↓
🧠 Domain: Entity 반환
  ↓
🎨 Presentation: UI 업데이트
```

---

## 유지보수가 쉬운 이유: 실무 시나리오

클린 아키텍처의 진짜 강점은 **변경에 강하다**는 점이다. 실제 상황을 살펴보자.

### 시나리오 1: 기획자 - "결제 처리 후 성공 토스트 메시지를 보여주세요"

#### Before (클린 아키텍처 없을 때)

```js
// ❌ 여러 컴포넌트를 찾아서 각각 수정해야 함
// components/ProcessPaymentButton.jsx
const handleClick = async () => {
  const response = await axios.post("/api/process-payment", data);
  toast.success("결제를 처리했다"); // ← 추가
};

// components/QuickPaymentButton.jsx
const handleQuickPayment = async () => {
  const response = await axios.post("/api/quick-payment", data);
  toast.success("빠른 결제를 처리했다"); // ← 추가
};

// components/RecurringPaymentButton.jsx
const handleRecurring = async () => {
  const response = await axios.post("/api/recurring-payment", data);
  toast.success("정기 결제를 처리했다"); // ← 추가
};
```

#### After (클린 아키텍처)

```ts
// ✅ Hook 1개만 수정하면 모든 컴포넌트에 적용됨
// presentation/hooks/useProcessPayment.ts
export function useProcessPayment(
  gateway: PaymentGateway,
  processPaymentUseCase: ProcessPaymentUseCase
) {
  return useMutation<User, Error, ProcessPaymentParams>({
    mutationFn: async ({ userId, amount }: ProcessPaymentParams) => {
      return await processPaymentUseCase.execute(gateway, userId, amount);
    },
+   onSuccess: () => {
+     toast.success("결제를 처리했다");
+   },
  });
}
```

**변경 범위**: Hook 1개 파일 (모든 결제 처리 버튼에 자동 적용)

---

### 시나리오 2: 서버 - "API 응답 구조가 바뀌었어요"

서버가 `transaction_id` 대신 `id`를 보내기 시작했다.

#### Before (클린 아키텍처 없을 때)

```js
// ❌ 모든 컴포넌트를 수정해야 함
// components/ProcessPaymentButton.jsx
const transactionId = response.data.transaction_id; // ← 여기
alert(`결제 처리 완료! ID: ${transactionId}`);

// components/PaymentHistory.jsx
const id = item.transaction_id; // ← 여기도

// components/TransactionDetail.jsx
const transactionId = transaction.transaction_id; // ← 여기도
```

#### After (클린 아키텍처)

```diff
// ✅ Data Layer의 Mapper 함수만 수정
// data/mappers/paymentTransactionMapper.ts
  export function mapPaymentTransactionDtoToDomain(dto: PaymentTransactionDto): PaymentTransaction {
    return new PaymentTransaction({
-     id: dto.transaction_id,
+     id: dto.id,
      gatewayId: dto.gateway_id,
      userId: dto.user_id,
      amount: dto.amount,
      // ...
    });
  }
```

**변경 범위**: Data Layer의 Mapper 함수 1개 파일

나머지 코드는 `PaymentTransaction` Entity의 `id` 속성만 쓰므로 **아무 영향을 받지 않는다**.

---

### 시나리오 3: 기획자 - "환불 최소 금액을 5,000원에서 10,000원으로 바꿔주세요"

#### Before (클린 아키텍처 없을 때)

```js
// ❌ 컴포넌트 곳곳의 if문을 찾아서 수정
// components/RefundButton.jsx
if (amount < 5_000) {
  alert("최소 환불 금액은 5,000원입니다");
}

// components/RefundForm.jsx
if (payment < 5_000) {
  return "최소 5,000원 이상";
}

// components/PaymentInput.jsx
<input min={5_000} />;
```

#### After (클린 아키텍처)

```diff
// ✅ Domain Layer의 Entity 1줄만 수정
// domain/entities/User.ts
- static readonly MIN_REFUND_AMOUNT = 5_000;
+ static readonly MIN_REFUND_AMOUNT = 10_000;
```

**변경 범위**: Domain Layer의 Entity 1줄

모든 곳에서 `User.MIN_REFUND_AMOUNT`를 참조하므로 **자동으로 적용**된다.

---

### 시나리오 4: 개발자 - "테스트 코드를 작성하고 싶어요"

#### Before (클린 아키텍처 없을 때)

```js
// ❌ 테스트 복잡함 (API, 서버 응답 형식, 엔드포인트 등 모두 모킹 필요)
// import { test } from "vitest";
test("환불 버튼 클릭 시 환불", async () => {
  // axios 모킹 → 서버 응답 형식 → snake_case 처리 → ...
  // 비즈니스 로직만 테스트하고 싶은데 API 의존성이 너무 많다
});
```

#### After (클린 아키텍처)

```ts
// ✅ Domain Layer 독립 테스트 가능
import { test, expect } from "vitest";
import { User } from "@/domain/entities/User";

test("최소 환불 금액 미만이면 환불 불가", () => {
  const user = new User({
    id: 1,
    name: "김사용자",
    gatewayId: 1,
    paymentBalance: 8_000,
    walletBalance: 0,
    createdAt: new Date(),
    updatedAt: new Date(),
  });

  expect(user.canRefund(8_000, 10_000)).toBe(false);
});
```

```ts
// ✅ UseCase 테스트 (Repository 모킹)
test("예치금 부족 시 에러", async () => {
  const mockRepository = { processPayment: vi.fn() };
  const useCase = new ProcessPaymentUseCase(mockRepository);
  const gateway = new PaymentGateway({
    id: 1,
    name: "테스트결제대행사",
    walletBalance: 3_000,
    createdAt: new Date(),
    updatedAt: new Date(),
  });

  await expect(useCase.execute(gateway, 1, 5_000)).rejects.toThrow(
    "예치금이 부족하다"
  );
});
```

**변경 범위**: 테스트만 추가 (기존 코드 수정 불필요)

---

### 시나리오 5: 기획자 - "예치금 충전 기능을 추가해주세요"

새로운 기능을 추가할 때 클린 아키텍처의 진가가 드러난다.

#### Before (클린 아키텍처 없을 때)

```js
// ❌ 여러 곳을 수정해야 함
// components/DepositButton.js
const handleDeposit = async () => {
  // 충전 로직을 처음부터 작성
  if (depositAmount <= 0) {
    alert("충전 금액은 0보다 커야 합니다");
    return;
  }

  const response = await axios.post(`/api/payment-gateways/deposit`, {
    payment_gateway_id: gatewayId,
    amount: depositAmount,
  });
  // ...
};

// components/QuickDepositButton.jsx
// 똑같은 충전 로직 복사...

// components/WalletPage.jsx
// 또 똑같은 충전 로직 복사...
```

#### After (클린 아키텍처)

클린 아키텍처에서는 **각 레이어에 필요한 부분만 추가**하면 된다.

```ts
// UseCase 추가
export class DepositToGatewayUseCase {
  async execute(gateway: PaymentGateway, amount: number) {
    if (amount <= 0 || amount > 10_000_000) {
      throw new Error("충전 금액은 0원~1,000만원이어야 합니다");
    }
    return await this.paymentGatewayRepository.deposit(gateway.id, amount);
  }
}

// Hook 추가
export function useDeposit(gateway, depositUseCase) {
  return useMutation({
    mutationFn: ({ amount }) => depositUseCase.execute(gateway, amount),
    onSuccess: (updated) =>
      queryClient.setQueryData(["gateway", updated.id], updated),
  });
}
```

**변경 범위**: 각 레이어에 1개 파일씩 추가 (기존 코드 수정 없음). 로직을 복사하지 않고 재사용한다.

---

### 시나리오 6: 개발자 - "결제와 환불을 동시에 누르면 상태가 꼬여요"

실제 서비스에서 자주 발생하는 문제다. 사용자가 "결제" 버튼을 누르고, 응답이 오기 전에 "환불" 버튼을 또 누르면 어떻게 될까?

#### 문제: 클라이언트가 상태를 직접 변경하면 동기화 문제 발생

```ts
// ❌ Entity가 직접 상태를 변경 → 서버와 불일치
user.receivePayment(5_000); // 클라이언트: 15,000
// 서버 응답 (수수료 차감): 14,950 → 상태 불일치!
```

#### 해결: Entity는 readonly, 서버가 상태 결정

```ts
// ✅ Entity는 검증만 수행
class User {
  private readonly _paymentBalance: number;
  canRefund(amount: number, minRefundAmount: number): boolean {
    return this._paymentBalance >= amount && amount >= minRefundAmount;
  }
}

// UseCase는 서버 응답만 사용
const processPaymentUseCase = async (user: User, amount: number) => {
  PaymentGateway.validatePaymentAmount(amount);
  return await api.processPayment(user.id, amount);
};
```

TanStack Query가 서버 응답으로 캐시를 자동 업데이트한다.

**핵심**: **"Tanstack Query가 상태 관리를 전담한다"**. Entity에서도 상태를 관리하면 Tanstack Query 캐시와 불일치가 발생한다. 따라서 Entity는 불변(`readonly`)으로 만들어 검증만 담당한다.

| 구분               | Mutable Entity (❌)                   | Immutable Entity (✅)               |
| ------------------ | ------------------------------------- | ----------------------------------- |
| **상태 관리 주체** | Entity + Tanstack Query (중복)        | Tanstack Query만                    |
| **상태 불일치**    | Entity와 캐시가 다를 수 있음          | 발생하지 않음 (캐시만 상태 관리)    |
| **동기화 방식**    | 수동 (서버 응답을 Entity에 반영 필요) | 자동 (Tanstack Query가 캐시 갱신)   |
| **동시 요청 처리** | Race Condition 발생 가능              | Tanstack Query가 순차 처리          |
| **디버깅**         | 어디서 상태가 바뀌었는지 추적 어려움  | Tanstack Query DevTools로 추적 쉬움 |

---

### 유지보수 비용 비교

| 변경 사항           | Before (안티패턴)                           | After (클린 아키텍처)                |
| ------------------- | ------------------------------------------- | ------------------------------------ |
| 버튼 색상 변경      | 여러 컴포넌트 수정                          | Presentation 레이어만                |
| API 응답 형식 변경  | 서버에서 변경된 필드명 모든 컴포넌트 수정   | Data 레이어(Mapper)만                |
| 최소 금액 규칙 변경 | 컴포넌트마다 하드코딩된 검증 로직 수정      | Domain 레이어(Entity) 상수 1줄       |
| 테스트 코드 작성    | 컴포넌트가 API/전역 상태 의존으로 모킹 복잡 | Domain 레이어 순수 함수 단위 테스트  |
| 환불 기능 추가      | 여러 컴포넌트에 중복 로직 복사              | 단일 책임에 맞게 레이어별로 추가     |
| 새 결제 수단 추가   | 전체 컴포넌트에서 분기 처리 추가            | Data 레이어(DataSource/Repository)만 |
| axios → fetch 변경  | 모든 API 호출 코드 수정                     | Infrastructure 레이어(HttpClient)만  |

---

## 도입 후 기대 효과

우리 팀은 기존 `Feature Based Architecture`에서 `Domain Based Clean Architecture`로 전환했다.

팀원들에게 위에서 설명한 장점들을 정리해서 전달했다: "서버 필드명이 바뀌어도 `Mapper` 1줄만 고치면 된다", "비즈니스 로직이 `Entity`에 모여 있어 테스트가 쉽다", "레이어가 명확해 AI가 정확한 위치에 코드를 생성한다".

특히 우리 서비스는 `Next.js` 기반이라 서버 환경과 클라이언트 환경을 모두 고려해야 했는데, 레이어가 명확할수록 개선과 리팩토링이 훨씬 수월했다.

사실 제일 걱정했던 부분은 내가 이 아키텍처 변경을 주도한 만큼, 기존 코드베이스 전체를 전환해야 한다는 책임감이었다. 하지만 AI Prompt를 활용해 전환했더니 **생각 이상으로 빠르게 성공**했다.

처음엔 팀원들도 "조금 과한 설계 아닌가?" 싶어했지만, 변경이 잦은 우리 서비스 환경(금융사 정책 변경, API 응답 형식 변경)을 고려하면 왜 이 구조가 필요한지 설명했더니 이해하기 시작했다.

### 코드 리뷰가 오히려 더 쉬워질 것이다

파일 개수는 2~3배 늘어나지만, 레이어가 명확하기 때문에 **필요한 부분만 선택적으로 볼 수 있다**. 비즈니스 로직 변경이라면 `Domain/Entity`만 보면 되고, API 변경이라면 `Data/DataSource`만 보면 된다.

특히 **테스트 코드 위주로 리뷰**하는 것이 가능해진다. 구조가 테스트하기 좋게 설계되어 있다 보니, 팀원들이 테스트 코드를 더 많이 작성하게 되었다. 리뷰어는 테스트 코드를 먼저 보고 "이 케이스도 추가하면 좋겠어요", "이 엣지 케이스는 어떻게 처리하나요?" 같은 본질적인 질문을 할 수 있다. 코드 품질이 자연스럽게 높아질 것으로 기대한다.

### AI가 정확한 위치에 코드를 생성한다

아키텍처 구조를 프롬프트에 명확히 작성해두면, AI가 **정확히 어느 레이어에 어떤 코드를 작성해야 하는지** 이해한다. 다음은 실제로 사용하는 AI 프롬프트 예시다:

```
# CLEAN ARCHITECTURE RULES

## DI Layer (Dependency Injection)
- MUST: Use singleton pattern (??= operator) for all instances
- MUST: Provide factory methods (static getters) for dependencies
- MUST NOT: Contain any business logic
- MUST NOT: Directly instantiate dependencies outside of getters

## Core Layer (Shared Utilities)
- MUST: Be pure, reusable utility functions
- MUST: Have zero dependencies on other layers
- MUST NOT: Import from Domain, Data, Infrastructure, or Presentation
- MUST NOT: Contain framework-specific code

## Infrastructure Layer (External Adapters)
- MUST: Implement port interfaces defined in Domain
- MUST: Handle all external system communication (HTTP, Storage, etc.)
- MUST NOT: Know about Domain entities or business rules
- MUST NOT: Transform data formats (use Data layer Mappers instead)

## Data Layer (Data Access & Transformation)
- MUST: Implement Repository interfaces from Domain
- MUST: Use Mapper to convert DTO ↔ Entity
- MUST: Handle all DTO type definitions
- MUST NOT: Contain validation or business logic
- MUST NOT: Skip Mapper and directly pass DTO to Domain

## Domain Layer (Business Logic)
- MUST: Contain all business rules and validation in Entities
- MUST: Define Repository interfaces (ports)
- MUST: Be completely independent (no imports from other layers)
- MUST NOT: Call APIs or access external systems directly
- MUST NOT: Import from Data, Infrastructure, or Presentation layers

## Presentation Layer (UI & Hooks)
- MUST: Use hooks to consume UseCases
- MUST: Handle UI state and user interactions only
- MUST NOT: Contain business logic or validation
- MUST NOT: Call Repository or DataSource directly
- MUST NOT: Import DTOs (only work with Entities)

---

Example Task:
"Add minimum payment amount validation logic"

Expected AI Response:
- File: `src/Domain/Entity/PaymentGateway.ts`
- Action: Add validation method to Entity class
- Reason: Business rule MUST be in Domain Layer

Example Task:
"Server changed response format: payment_id → id"

Expected AI Response:
- File: `src/Data/Mapper/PaymentGatewayMapper.ts`
- Action: Update single line in Mapper
- Reason: DTO transformation MUST happen only in Mapper
```

폴더 구조가 명확하지 않으면 AI가 어디에 코드를 넣을지 헤매지만, 이렇게 **레이어별 MUST/MUST NOT 규칙**을 명시하면 AI가 **개발자가 의도한 대로, 정해진 틀 내에서 기능을 추가하고 테스트 코드를 작성**할 수 있다. 아키텍처 가이드라인을 벗어나지 않으면서도 빠르게 개발할 수 있는 것이다.

### 장기적으로 기대하는 효과

- **심리적 안정감**: "이거 고치면 어디 터질까" 불안감 소멸. 변경 범위가 명확하므로 배포가 두렵지 않음
- **온보딩 시간 단축**: 새 팀원이 구조만 이해하면 바로 기여 가능. "이 기능은 어디 있나요?" 질문이 줄어들 것
- **테스트 커버리지 증가**: `UseCase`는 모킹 없이 테스트 가능. 테스트 작성이 즐거워져서 커버리지가 자연스럽게 높아질 것
- **AI 활용도 증가**: 테스트 코드가 늘어나니 조금 더 편하게 AI를 활용할 수 있다. "내가 돌린 AI 때문에 기존 코드가 무너지지 않을까?" 하는 걱정 없이 AI를 활용할 수 있는 것이다

### 균형잡힌 평가: 만능은 아니다

클린 아키텍처가 만능은 아니다. **작은 프로젝트**나 **일회성 프로토타입**에는 과할 수 있다. 초기 비용이 크기 때문이다.

하지만 **우리 서비스**처럼 외부 정책 변경이 잦고, 팀원이 여럿이며, 나처럼 프론트엔드와 서버 개발을 함께 하거나 장기 운영이 예정되어 있다면 초기 비용을 감수할 가치가 충분하다.

---

## 핵심 정리

클린 아키텍처를 한 문장으로 정리하면: **역할을 분리해서 변경에 강한 구조를 만드는 것**이다.

### 6개 레이어와 역할

코드를 6개 레이어로 나눠 각자의 책임을 명확히 한다:

- **`DI`** (의존성 주입) - 모든 레이어를 조합하고 주입
- **`Core`** (공통 기능) - 모든 레이어에서 쓰는 순수 유틸리티
- **`Infrastructure`** (외부 세계 격리) - HTTP 클라이언트, 스토리지 등 외부 도구 추상화
- **`Data`** (외부 데이터 변환) - API 호출 + DTO ↔ Entity 변환
- **`Domain`** (비즈니스 규칙) - 검증 로직, 엔티티, 유스케이스 등 핵심 로직
- **`Presentation`** (UI 상호작용) - 훅과 컴포넌트로 UI 관리

### 핵심 원칙 3가지

1. `Domain`은 **완전히 독립** - 다른 레이어를 import하지 않음
2. **의존성은 항상 안쪽으로** - Presentation → Domain ← Data
3. **변환은 Mapper가 유일** - DTO와 Entity 사이는 Mapper만 통과

### 언제 도입하면 좋을까?

다음 상황이라면 클린 아키텍처가 빛을 발한다:

- 기획이 자주 바뀌는 서비스
- 6개월 이상 운영할 프로젝트
- 테스트 코드가 필요한 경우
- 프론트엔드와 백엔드를 함께 개발하는 경우

### 실제 유지보수 비용 비교

글 전체에서 다뤘던 결제 대행사(`PaymentGateway`) 예시로 실제 변경 비용을 비교해보자.

**시나리오 1: API 필드명 변경** (`wallet_balance` → `available_balance`로 변경)

- `Before (클린 아키텍처 없이)`: `ProcessPaymentButton.tsx`, `PaymentStatusCard.tsx`, `RefundModal.tsx` 등 `response.data.wallet_balance`를 사용하는 10개 컴포넌트를 모두 찾아 수정해야 한다. 하나라도 놓치면 런타임 에러가 발생한다
- `After (클린 아키텍처 적용)`: `PaymentGatewayMapper.ts` 1개 파일의 매핑 로직만 수정하면 된다. 모든 컴포넌트는 `PaymentGateway` Entity를 사용하므로 영향을 받지 않는다

```ts
// data/mappers/paymentGatewayMapper.ts
export function mapPaymentGatewayDtoToDomain(dto: PaymentGatewayDto) {
  return new PaymentGateway({
    // ...
-   walletBalance: validated.wallet_balance,  // 기존
+   walletBalance: validated.available_balance,  // 변경
  });
}
```

**시나리오 2: 비즈니스 규칙 변경** (최소 결제 금액 1,000원 → 5,000원으로 변경)

- `Before (클린 아키텍처 없이)`: 검증 로직이 `ProcessPaymentButton.tsx`, `QuickPaymentForm.tsx`, `PaymentAmountInput.tsx` 등 5개 컴포넌트에 중복되어 있다. 각 파일을 찾아 `1_000`을 `5_000`으로 수정해야 한다
- `After (클린 아키텍처 적용)`: `PaymentGateway.ts` Entity의 `canProcessPayment()` 메서드 **1줄**만 수정하면 된다

```ts
// domain/entities/PaymentGateway.ts
canProcessPayment(amount: number): boolean {
- const MIN_PAYMENT = 1_000;  // 기존
+ const MIN_PAYMENT = 5_000;  // 변경
  return this._walletBalance >= amount && amount >= MIN_PAYMENT;
}
```

**시나리오 3: 외부 API 변경** (결제 대행사 API v1 → v2로 업그레이드)

- `Before (클린 아키텍처 없이)`: API를 호출하는 모든 컴포넌트에서 엔드포인트(`/v1/payments` → `/v2/payments`)와 요청/응답 형식을 수정해야 한다. 각 컴포넌트마다 `fetch` 호출 부분을 찾아 고쳐야 한다
- `After (클린 아키텍처 적용)`: `PaymentGatewayDataSource.ts`와 `PaymentGatewayMapper.ts`만 수정하면 된다. `Domain`과 `Presentation` 레이어는 API 버전을 알 필요가 없으므로 변경이 필요 없다

이처럼 변경 범위가 명확하고 좁아진다. 마치 물류센터의 **한 구역만 개선**하면 되는 것과 같다.

---

## 마지막으로

클린 아키텍처는 초기 비용이 있다. 파일이 많아지고 구조가 복잡해 보인다.

하지만 장기적으로는 유리하다:

- **변경에 강하다**: API 응답 형식이 바뀌어도 `Mapper`만 수정하면 된다. 컴포넌트를 뒤지며 `response.data.xxx`를 찾아 수정할 필요가 없다
- **테스트가 쉽다**: `UseCase`는 의존성이 주입되므로 Mock Repository만 전달하면 순수 함수처럼 테스트할 수 있다. "이 함수 테스트하려면 뭘 모킹해야 하지?" 고민이 사라진다
- **AI 활용이 좋다**: 레이어별 규칙이 명확하므로 프롬프트를 명확하게 작성하면 AI가 정해진 틀 안에서 코드를 생성한다. "AI가 아키텍처를 망가뜨리면 어쩌지?" 걱정 없이 코드 생성을 맡길 수 있다
- **코드 수정의 두려움이 사라진다**: 변경 범위가 명확하므로 "이거 고치면 어디 터질까?" 걱정 없이 배포할 수 있다

결국 팀 전체의 만족도가 높아진다. "이거 고치면 어디 터질까", "기획이 변경되면 어떻게 해야 할까"에 대한 걱정을 완전히 없애지는 못하지만, 어느 정도 줄이며 개발할 수 있는 것.

이것이 견고한 아키텍처의 가장 큰 가치라고 생각한다.
