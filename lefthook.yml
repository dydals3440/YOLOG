pre-commit:
  piped: true
  jobs:
    - name: oxlint
      glob: "*.{js,mjs,cjs,jsx,ts,tsx}"
      run: bunx oxlint --fix {staged_files}
      stage_fixed: true

    - name: oxfmt
      glob: "*.{js,mjs,cjs,jsx,ts,tsx,json,jsonc}"
      run: bunx oxfmt {staged_files}
      stage_fixed: true
